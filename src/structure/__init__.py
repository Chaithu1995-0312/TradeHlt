"""
structure — the governed CRT structural kernel.

One implementation of the geometry every CRT-shaped detector shares, so that research
programs consume the same arithmetic instead of re-deriving it. See
`docs/governance/` and ontology nodes SP-001 / SP-002 / SP-003.

Public surface (stable): `structure.predicates`.
"""
