<!-- ============================================================= -->
<!-- ARCHITECTURE MIGRATION DOCTRINE — keep this block at the top.  -->
<!-- Canonical companion docs live in docs/architecture/.          -->
<!-- ============================================================= -->

# ARCHITECTURE MIGRATION DOCTRINE

> **This file is the permanent record of every session decision since April 2026.**
> Every entry below carries a date and a decision summary in timestamped order, so you can scroll back through the full history without replaying old conversations.
> Governed by [`CLAUDE.md`](CLAUDE.md) §6 (Persistent Logging Mandate).
> Cross-reference: [`CLAUDE.md`](CLAUDE.md) §2 for the companion docs map.
---

**North star — LLM context economy.** We migrate toward an event-driven LLM-event-
microservices architecture so a future LLM loads only the *one* service it needs
(ins → flow → outs) instead of the whole codebase. Microservice boundaries = context
separation. Telemetry is curated into per-episode "LLM logs" so context does not grow
unbounded. Every application flow is documented as a loadable context unit.

**Priority order (ranks above refactor speed):**
replay correctness > explainability > telemetry continuity > advisory-AI >
(structure validity ≠ execution validity).

**The five governance questions — apply as a pre-merge checklist to every change:**
1. Does replay remain deterministic?
2. Does telemetry remain comparable across runs?
3. Can this state be audited later?
4. Can an LLM reason about this event?
5. Is execution authority still isolated?

**Standing rules:**
- **Consolidate on `src/events/event_fabric.py` — never fork the event system.**
- LLMs are advisory governance, **never** execution authority.
- No lookahead in replay; deterministic seeds mandatory; comparison ignores
  `event_id`/`generation`/wall-clock `timestamp`.
- New telemetry is additive; no field removed without a documented superseding field.

**Migration sequencing index** (detail in `docs/implementation_plan/`):
- **M0** — Map & doctrine: `docs/architecture/{CODEBASE_STATE_MAP,EVENT_TAXONOMY,
  SERVICE_BOUNDARY_MAP,REPLAY_GOVERNANCE,LLM_GOVERNANCE_LAYER}.md` + `services/`.
- **M1** — Telemetry normalization (envelope trade writers) + per-episode LLM log.
- **M2** — Event extraction (CRTState transitions + silent EventTypes).
- **M3** — Orchestration de-coupling (kill `live_engine_hook` singletons).
- **M4** — Dependency inversion (governance/analytics stop importing `runtime.backtest_v2`).
- **M5** — LLM-layer hardening (`GOVERNANCE_MODE`, decision-path assertions, advisory event contract).

<!-- ============================================================= -->
<!-- SESSION LOG (newest first). Append new entries below the       -->
<!-- doctrine block, above prior entries.                           -->
<!-- ============================================================= -->

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Migration - Batch A (4 live-spine modules) + Batch B (behavior_census evidence engine). Practice->Evidence; doctrine NOT yet frozen into CLAUDE.md.
Decision/Output: Migrated 4 live-spine modules from hardcoded BEHAVIORAL constants to config, FAIL-FAST (owner directive "no defaults, throw error, fail fast") - NOT R2 .get(default) soft pattern. A1 dynamic_threshold.py/decision_engine.py: _THRESHOLD_PERCENTILE/MIN/MAX -> decision_engine.threshold_* (resolved a doc/code split-brain: header CLAIMED config-driven, code was hardcoded; DynamicThreshold params now required). A2 regime_governor.py -> new regime_governor section + from_prod_config (INERT: ultron_gate_enabled=false). A3 convergence_controller.py -> new convergence_controller section + from_prod_config (INERT: fusion_use_evaluate=false). A4 acceptance_controller.py -> new acceptance_controller section + from_prod_config. All hash-neutral (params untouched). Built scripts/analysis/behavior_census.py (AST classifier STRUCTURAL/BEHAVIORAL/GOAL_SEEKING + config-wired detection; 67 behavioral/45 future opportunities) + tests/test_behavior_census.py regression floor. Staging doctrine docs/research-readiness/config-first-doctrine.md (NOT CLAUDE.md). PARITY BYTE-IDENTICAL on BNBUSDT f61b44ea...e4e9 + SOLUSDT d2721c2c...b591d (baseline==post-A1==combined). 118 module tests + 6 census/reachability green. 1 unrelated red (test_engine_runner_ml_impl, ML deps absent).
Belief Update / ROI / Goal: Goal: make behavior config-mutable so goal-seeking = generating configs, not editing engines. Belief: config-first is provable WITHOUT behavior drift - fail-fast + JSON-value==prior-literal gives byte-identical ledgers; A2/A3 confirmed currently inert. Knowledge ROI: high - established a repeatable from_prod_config strict-boundary pattern + an evidence engine (behavior_census) turning the doctrine into a test floor. Action: hold doctrine in STAGING until >=4 proven migrations + green census (now met); Batch C (CRTConfig ~30 params knobs, needs rehash) deferred.
Open Questions: confirm full determinism+oracle suite green (running at log time, expected green). When to freeze sec.2-4 into CLAUDE.md (criterion now met - owner call).
Next Step: confirm determinism/oracle; offer owner (a) freeze doctrine into CLAUDE.md, (b) Batch C CRTConfig split-brain, or (c) next census batch (crt_gaussian_scorer hard-filters).
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 2 - FREEZE doctrine into CLAUDE.md sec.6.5 + upgrade behavior_census with a maturity model. Owner directive: freeze now (before Batch C), GOAL_SEEKING orthogonal, strong fail-fast rule.
Decision/Output: Froze the Config-First doctrine into CLAUDE.md new sec.6.5 (doctrine cluster, between 6.4 and 7): principle (Frozen Engine + Mutable Behavior + Governed Evolution); META-RULE "Evidence outranks doctrine" placed immediately after principle (freeze only after implementation->parity->census->determinism, never reverse); 3 classes STRUCTURAL/BEHAVIORAL/GOAL-SEEKING; maturity ladder HARD_CODED->CONFIG_WIRED->CONFIG_DRIVEN with GOAL_SEEKING ORTHOGONAL (CONFIG_DRIVEN->GOAL_SEEKING, not every module a tuner); hard rule NO silent defaults (strict _require/from_prod_config, missing key=error, soft .get deprecated, parity=JSON==literal + strict read + byte-identical); 6 questions; enforcement contract behavior_census.py <-> test_behavior_census.py; Batch C note. Flipped staging doc header STAGING->FROZEN(2026-06-15). Upgraded behavior_census.py (additive, read-only): per-module maturity + maturity_summary + orthogonal is_goal_seeking marker + maturity table in MD; always-include tracked migrated modules; check_migrated now pins maturity. Verified: maturity CONFIG_DRIVEN 10 / CONFIG_WIRED 6 / HARD_CODED 6; dynamic_threshold=CONFIG_DRIVEN, regime/convergence/acceptance=CONFIG_WIRED (owner table exact). Tests: 7 census/reachability + 18 doc/findings-enforcement green. No code-path change (census read-only, CLAUDE.md prose) -> determinism/oracle untouched, no re-fingerprint. No F-id (engineering doctrine, not a system finding).
Belief Update / ROI / Goal: Goal: make the proven config-first philosophy CONSTRAIN future work instead of waiting on Batch C. Belief: doctrine has crossed from aspiration to experimentally-verified repository truth (4 byte-identical migrations + census + determinism all green) - so freezing is harvesting the lesson, not premature. Knowledge ROI: high - sec.6.5 + the maturity ladder turn "magic-number count" into a maturity model that future migrations (incl. Batch C) inherit; "Evidence outranks doctrine" is the transferable meta-rule. Action: doctrine frozen; Batch C (CRTConfig ~30 params knobs, needs rehash) becomes the FIRST consumer of the doctrine, not its justification.
Open Questions: none blocking. Batch C sequencing (when owner wants it).
Next Step: Batch C - CRTConfig params split-brain repair under the now-frozen sec.6.5 (rehash via scripts/maintenance/_compute_hash.py); or next census HARD_CODED batch (crt_gaussian_scorer hard-filters). Owner's call.
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 3 - evidence-guided prioritization: extra doctrine (precedence hierarchy + authority ladder), SHARPEN behavior_census (external-injection blind spot), migrate PROMOTION_MARGIN, re-rank. Owner sequence: fix the biased instrument BEFORE migrating.
Decision/Output: WI-0 froze into CLAUDE.md sec.6.5 + staging doc: precedence hierarchy Evidence>Doctrine>Preference>Convenience>Elegance; "Evidence quality outranks migration quantity"; "Never optimize using a biased measurement instrument"; AUTHORITY LADDER (anti-self-deception, generalizes beyond config, ties sec.6.1+G001): permanent invariant "evidence has no authority; only demonstrated G001 improvement grants authority"; 4 distinct states Information->Economic usefulness->Authority earned->Architecture justified; corollaries Information!=value, value!=authority, authority!=architecture; REGIME_EXPLOITABLE=information not authority, only CONSUMER_CANDIDATE w/ measured dG001 earns authority; CONFIG_DRIVEN grants tunability not authority. WI-1 sharpened behavior_census: corpus config-key-read scan + curated _CONFIG_SCHEMA_CLASSES{CRTConfig,FusionConfig} + *Result/Record field defaults=STRUCTURAL placeholders; per-const externally_wired/hardcoded split. EFFECT: future config opportunities 45->7, HARD_CODED 6->5, fusion_engine HARD_CODED->CONFIG_WIRED. MAJOR CORRECTION: crt_engine_v2 "27" = 24 externally-wired CRTConfig fields + 3 genuine state defaults => "Batch C" is config-COMPLETENESS (populate active params/crt_engine + rehash) NOT engine surgery. WI-2 migrated model_registry.PROMOTION_MARGIN=0.02 -> governance.promotion_margin via ModelRegistry.from_prod_config() fail-fast (eager singleton); value unchanged 0.02; 91 model_registry/promotion tests green. WI-3 recorded sharpened ranking in staging doc (A5 row + Phase-3 section). Verified: 26 census/reachability/doc-enforcement green; determinism spot-check BNBUSDT f61b44ea...e4e9 BYTE-IDENTICAL (governance/census edits don't touch spine). New test asserts fusion_engine not over-reported.
Belief Update / ROI / Goal: Goal: use the census AS INTENDED (evidence->prioritization->migration) instead of picking Batch C by habit. Belief: the prioritization instrument was BIASED (over-reported HARD_CODED via external-injection blind spot) - fixing it first was higher-leverage than any migration; the genuine backlog is tiny (7 opps) and Batch C is much smaller/different than feared (config-completeness, not 30-knob engine surgery). Knowledge ROI: high - sharpened census now gives trustworthy rankings for all future migrations; PROMOTION_MARGIN harvested as the clean genuine win; authority ladder is the transferable anti-self-deception doctrine. Action: trustworthy backlog established; remaining genuine HARD_CODED are advisory/dormant/3-state-defaults; Batch C = config-completeness task (owner's call, needs rehash).
Open Questions: Batch C (CRTConfig active-config completeness + rehash) timing - owner's call. The 3 crt_engine_v2 state defaults (decay_factor/risk_pct/soft_conf_candles) - migrate or leave (low value).
Next Step: owner decision: Batch C config-completeness (populate params/crt_engine + rehash) OR sweep remaining tiny HARD_CODED OR stop (backlog now trustworthy + small).
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 4 (Batch C) - CRT config-completeness, REFRAMED by Phase-3 instrument repair: NOT engine surgery, NOT rehash. Hash-neutral crt_engine population + state-default classification.
Decision/Output: Investigation: load_prod_config builds CRTConfig={**crt_engine,**params}, hash covers params ONLY -> crt_engine is the unhashed defaults layer. Exact gap = 12 CRTConfig fields absent from crt_engine+params (of 48), each resolving to dataclass default. WI-C1: populated all 12 into crt_engine with value==dataclass default (breakout_disp_threshold 1.5, exit_model 'intrabar_touch' [load-bearing governing exit, previously IMPLICIT], score_threshold 0.45, use_bitnet false, extension_reset_fib 1.618, pending_displacement_ttl_candles 4, max_expansion_age_candles 495/hours 124, expansion_age_warn_candles 342, shadow_advisory_only false, shadow_age_norm_candles 0, shadow_age_penalty_lambda 0.0). HASH-NEUTRAL, NO rehash (owner: crt_engine=defaults layer, params=tuned/hashed). allowed_sessions EXCLUDED (engine_runner-owned via resolve_allowed_sessions; would create new split-brain). PARITY BYTE-IDENTICAL BNB f61b44ea...e4e9 + SOL d2721c2c...b591d. WI-C2: 3 state defaults (RiskScore.decay_factor/Trade.risk_pct/EngineState.soft_conf_candles) classified STRUCTURAL placeholders (real knobs score_decay_lambda/sizing_bands/soft_conf_max_candles already config-driven) via census _RESULT_SCHEMA_RE+State + _RUNTIME_STATE_CLASSES -> crt_engine_v2 genuine HARD_CODED 3->0 -> CONFIG_WIRED. WI-C3: added §6.5 + staging-doc corollary "correcting the instrument often creates more value than optimizing the system". WI-C4: census re-run HARD_CODED 5->3, opportunities 7->3 (only signal_audit advisory + signal_belief_tracker sidecar + HMF dormant remain). Verified: config loads+hash OK; 27 census/reachability/doc-enforcement green; new crt_engine_v2 + fusion_engine census assertions green. determinism+oracle suite running (corroboration; fingerprint already byte-identical).
Belief Update / ROI / Goal: Goal: close the F-018 CRT split-brain (config = explicit source of truth). Belief: Batch C was MUCH smaller than the original "30-knob engine surgery + rehash" fear - the instrument repair revealed it as a 12-field hash-neutral config-completeness pass; the program has reached its meaningful END (every live behavior-affecting knob now config-driven; remaining 3 are advisory/sidecar/dormant). Knowledge ROI: high - "Batch C needs rehash" belief RETIRED (evidence outranks expectations); the governing exit_model is now explicit in config (was implicit). Action: config-first migration program COMPLETE for live knobs; remaining HARD_CODED are intentionally skipped (advisory/sidecar/dormant).
Open Questions: none blocking. Optional micro-items: signal_audit warn thresholds (advisory) if ever wanted; otherwise STOP - backlog is intentionally empty of high-value items.
Next Step: program effectively done. Optional: commit the work; or migrate the 3 advisory/sidecar knobs only if their modules become live (don't - F-012 theater). Goal-seeking (generating candidate configs over the now-complete crt_engine) is the natural next CAPABILITY, separate from migration.

---
📝 SESSION LOG ENTRY
Date: 2026-06-25 17:05 IST
Topic: Gate-ON zone ablation closes Option B — zone is non-pivotal on the REAL fusion spine (redundant/decision-dominated, NOT weak); F-036 confidence restored Likely
Decision/Output: Re-ran diagnose_zone_inertness.py with BACKTEST_ENGINE_GATE=1 (shell-set; dotenv override=False so it sticks) → results/research/zone_inertness_gateon/. The fusion gate now RUNS (audit_bars=13, real engine scores; self-check byte-identical; baseline reproduces gate-ON impact BNB11/ETH4/BTC5/SOL6). ABLATION: weight_zone_gate∈{0,0.2,0.4,0.6} × bitnet_zone_threshold∈{0,0.25,0.5} → EVERY cell byte-identical ⇒ ZONE_NON_PIVOTAL (real, not gate-off artifact). PHASE-2 MECHANISM (real audit): zone NOT weak (mean 0.608, median 0.718, +0.358 above the 0.25 threshold, ≥0.25 on 85% of bars) and NOT under-weighted (3× weight = 0 change) → REDUNDANT / DECISION-DOMINATED: zone is a near-constant strong "yes" vote that never moves the fused score across the DecisionEngine cut; the real fusion vetoes (CRT 13→11 etc.) are zone-INDEPENDENT (CRT/Gaussian/RR/decision drive them; engine means crt 0.339 < zone 0.608 < gaussian/rr 0.747). Updated F-036 (live-path now TESTED, confidence Possible→Likely restored — non-pivotality is byte-exact/deterministic, robust to tiny N; the interim "fusion weight 0.2 flips no decision" overclaim is now re-measured gate-ON and SUPPORTED) + F-037 (gate-ON ablation ran, vetoes zone-independent). CLAUDE.md index synced; test_current_findings + test_doc_citations 10 green.
Belief Update / ROI / Goal: Goal: honest, live-equivalent answer to "why is zone inert." Belief (closed): zone is inert NOT from weak scores and NOT from under-weighting — it's REDUNDANT in the fusion (always-on strong vote, non-discriminating); the decision is set by CRT+Gaussian+RR. Knowledge ROI: high — the Option-B diagnosis is complete AND it's now measured on the correct (gate-ON) object after the instrument-scope correction. Action: do NOT pursue zone-weight tuning or zone-quality work for throughput on the strength of more-weight (proven null); any zone authority would require a DIFFERENT construction (discriminating zone signal), not a knob. Side flag for later: gaussian≡rr audit mean 0.7468 (possible rr_fusion coupling) — separate question, own evidence.
Open Questions: gaussian≡rr coupling (0.7468 identical) — is rr mirroring gaussian via rr_fusion? Not investigated. Does the zone redundancy hold in LIVE (vs the gate-ON backtest proxy)? Backtest gate-ON is the closest available proxy; true-live unmeasured.
Next Step: Zone thread is CLOSED (tunable, inert, redundant; default 3). Untracked artifacts: scripts/research/diagnose_zone_inertness.py, results/research/{zone_inertness,zone_inertness_gateon,_gate_impact}/ (results gitignored). Commit the script + doc/finding changes when the user asks.
---
📝 SESSION LOG ENTRY
Date: 2026-06-25 12:20 IST
Topic: E-001 CORRECTION — F-036 mechanism overclaim withdrawn; engine_runner FUSION GATE is disabled in backtest (.env BACKTEST_ENGINE_GATE=0)
Decision/Output: Investigating "why is the zone cluster inert" (Option B) via config-only ablation (scripts/research/diagnose_zone_inertness.py: weight_zone_gate∈{0,0.2,0.4,0.6} × bitnet_zone_threshold∈{0,0.25,0.5}, entry-set sha vs baseline, M4-on-change, + debug_mode signal_audit distribution). Ablation came back ALL byte-identical (verdict ZONE_NON_PIVOTAL) BUT Phase 2 audit = 0 bars → probed and found engine_runner.run() is called 0 times in the spine-adapter backtest. ROOT CAUSE: `.env` sets BACKTEST_ENGINE_GATE=0 (confirmed: not in shell env, 1 line in .env, loaded via dotenv; only backtest_v2.py honors it, live_engine_hook does NOT). So the 4-engine FUSION gate (CRT/Gaussian/zone/RR)+DecisionEngine veto NEVER RUNS in backtests — the entire research corpus (F-019/F-036/qualify_majors/diagnosis) measures the CRT-STATE-MACHINE-ONLY spine. Gate-ON probe (12k BNB): the lone CRT candidate is REJECTed (0 trades) vs gate-OFF 1 trade → the gate is highly consequential when on. CORRECTED F-036 (docs/current-findings.md + CLAUDE.md index, test_current_findings 8 green): withdrew the "zone fusion-weight 0.2 flips 0 decisions / EMA-gate-inert" mechanism claim as an overclaim; headline (ΔG001≡0-in-backtest) + tunability-NOT-authority verdict stand; live-path zone pivotality UNTESTED; Confidence Likely→Possible. DOC_DRIFT flagged: spine_signal_source.py docstring claims it runs the full CRT→Fusion→Decision→Ultron stack, but the gate-off config makes it CRT-only. User decisions: (1) "show gate-ON impact first" → full-history gate-ON vs gate-OFF BNB+SOL running in background; (2) "fix mechanism + downgrade confidence" → done.
Belief Update / ROI / Goal: Goal: honest mechanism for zone inertness + trustworthy research instrument. Belief CORRECTED: zone inertness in backtest is NOT a zone-score-weakness result — the fusion layer that would consume zone is disabled in the backtest harness; the live fusion gate is real and aggressive (rejected the only candidate). Knowledge ROI: VERY HIGH — caught a measurement-instrument defect (the research corpus measures a different spine than it documents) before it contaminated more findings; this is the §6.5 "correcting the instrument > optimizing the system" rule and an E-001 governance success (pre-contamination correction). Action: quantify gate-ON impact, then decide intended-vs-defect with the user; do NOT trust any "spine" finding's fusion-stack attribution until resolved.
Open Questions: Is BACKTEST_ENGINE_GATE=0 intended (research = CRT-only) or a latent defect (corpus should measure full fusion)? How many trades survive the gate-ON fusion veto over full history (impact run pending)? If gate-on yields ~0 trades, the full-fusion spine may be untradeable in backtest — separate question.
Next Step: Read gate-impact summary (BNB/SOL on/off trade counts + entry-set diff); file the corpus-scope finding (F-037 candidate: "research backtests are CRT-only; fusion gate disabled by .env"); update memory; surface intended-vs-defect to user.
---
📝 SESSION LOG ENTRY
Date: 2026-06-25
Topic: mt5_analytics v0.4.0 "Broker Semantics Verified" — frozen kernel proven broker-INDEPENDENT (hedging→netting), zero kernel change
Decision/Output: Placed tiny demo trades on the NETTING account (login 108830159, mm0) across normal/partial/pyramid/INOUT/reopen via the demo-gated generator (generalized --require-hedging→--require-margin {hedging,netting}; added `inout` keystone pattern). Rebuilt+verified+scored coverage in an isolated netting root (artifacts/reports/audit/netting, gitignored). verify PASS (net_pnl_diff 0.0, volume_diff 0.0, manifests OK); 6 position_ids→7 episodes. INOUT keystone: BUY 0.01→SELL 0.02→BUY 0.01 netted to position_id 9270517338 → 2 episodes (long, flipped short) split at DEAL_ENTRY_INOUT, correct dir/VWAP/net_pnl. Coverage: inout_reversals+pyramids+partial_closes OBSERVED (Silver 3/5); reopens REACHABLE_UNSEEN. Coverage-layer fix (NOT kernel): broker_semantics.json key now company|server|mm{margin_mode} (both demo accounts share company|server). 74 mt5_analytics tests green. Committed e4e02f3.
Belief Update / ROI / Goal: Goal: prove the reconstruction kernel is portable across broker margin models. Belief: CONFIRMED broker-independent — kernel handled netting (incl. INOUT reversal) correctly on first contact with ZERO change, now surviving hedging+netting + ~7 reality findings. New finding: MT5 netting assigns a FRESH position_id on re-open (so reopen ≠ id-reuse). Knowledge ROI: high — the sacred boundary is validated across the one structural axis (margin_mode) that most threatened it; everything above the kernel inherits this robustness. Action: kernel stays frozen; Gate-2 not triggered (no verify FAIL). Next capability work (dashboard/parquet) is information-not-authority per §6.5.
Open Questions: reopen-with-reused-position_id remains unexercised (broker assigns fresh ids) — REACHABLE_UNSEEN, honest. .env programmatic account-switching still deferred.
Next Step: Optional capability plugins (8 dashboard → 7 parquet/duckdb → gaussian/crt) per the 11-phase plan review order; none blocking.
---
📝 SESSION LOG ENTRY
Date: 2026-06-24 23:05 IST
Topic: Measured ΔG001 of the zone_gate config knobs (top_k/cluster_min_n/cluster_spread_max) → F-036
Decision/Output: Built scripts/research/qualify_zone_topk.py (measure-only): injects the cell's zone_gate knobs by wrapping config_layer.production_config.get_prod_section (deep-copy engine_runner, no JSON edit/rehash); one full-history spine backtest per (cell,instrument) feeds Layer 1 (the spine's own goal_report/G001 via GoalValidator.evaluate on intrabar_touch) AND Layer 2 (parse trades → canned SpineSignalSource → forward_walk → the 7-gate M4 QualificationGate vs always_long/random controls, intrabar_fixed/12bps/2000perm). Busted the registry-singleton cache by injecting a fresh canned spine source per cell. Stage A: top_k∈{1,2,3,5,8} × BNB/ETH/BTC/SOL. RESULT: V0 self-check byte-identical (injection neutral at defaults); spine entry sets BYTE-IDENTICAL at every top_k ⇒ ΔG001 ≡ 0 exactly (Δexpectancy_r=+0.0000 every cell×instrument; trades BNB13/ETH5/BTC5/SOL7). Zone cluster score (fusion weight_zone_gate=0.2) flips ZERO decisions across threshold — EMA-gate-inert "perfect no-op". M4: 0 PROMOTE (per-inst INSUFFICIENT n<30; POOLED n=30 E=−0.399 REJECT, identical to F-019). Determinism body_sha256=0e8221da. Filed F-036 (docs/current-findings.md + CLAUDE.md Repository Truths Index, test_current_findings 8 green). Stage B (cluster grid) NOT entered: no lead, cluster aggregation non-binding since top_k itself inert. Registry precondition checked: models/zone_registry.json LIVE (8 zones, weight 139,942 ≫ floor 50) so the null is a real measurement, not dormant-by-emptiness.
Belief Update / ROI / Goal: Goal: decide whether a non-default zone top_k can improve G001. Belief (was: "maybe the new knob helps") → CONFIRMED NULL: the zone cluster score is non-binding on the live spine; top_k is config-TUNABLE but economically INERT. Knowledge ROI: high — closes the §6.5 Authority-Ladder loop on the config migration (CONFIG_DRIVEN tunability earned, authority NOT earned) and adds a clean falsification (F-036) extending F-021/F-019. Action: do NOT sweep/tune zone top_k for throughput; default top_k=3 stays. Re-binding would require first raising weight_zone_gate or making zone a harder gate — a separate architecture decision needing its own evidence, not a knob sweep.
Open Questions: cluster_min_n/cluster_spread_max not directly swept (held at default) — but provably non-binding while top_k is inert. The spine's per-instrument E heterogeneity (SOL +0.99 n=7 vs BNB −0.36 n=13) is the same tiny-N "lead not edge" as F-019, not a new signal.
Next Step: Commit when the user asks (branch patch); new untracked files = scripts/research/qualify_zone_topk.py, tests/test_zone_gate_top_k.py, results/research/zone_topk_sweep/ (results gitignored).
---
📝 SESSION LOG ENTRY
Date: 2026-06-24 15:18 IST
Topic: Config-first migration of the ZoneGate top-k cluster aggregation (top_k / cluster_min_n / cluster_spread_max) + dead-path documentation
Decision/Output: Reframed a "install kWTA on the zone gate" discussion against the real code — the live spine ALREADY does top-3 weighted-cluster aggregation (engine_runner._zone_model_fn → compute_weighted_cluster_score(top_scores) >= bitnet_zone_threshold); the per-zone `allowed` threshold path in BitNetZoneGate.check() is computed-but-DEAD on the live spine. Externalized the 3 hard-coded BEHAVIORAL constants to a nested config section `engine_runner.zone_gate = {top_k:3, cluster_min_n:2, cluster_spread_max:0.15}` (idiomatic — mirrors existing rr_fusion/dual_engine/fusion_engine nesting). Threaded via fail-fast _cfg_require on the spine (engine_runner.__init__ resolves top_k → get_zone_gate(top_n=...); run() resolves cluster_min_n/spread_max → _zone_model_fn), soft default kept only for standalone/_smoke_test callers. live_engine.py: BitNetZoneGate._top_n slices check().top_scores; added clarifying comment that per-zone allowed is bypassed by the live spine (keep-and-document, NOT deletion — it's a tested public contract field). zone_gate_engine.py: compute_weighted_cluster_score(spread_max=0.15) param. Mirrored block into ENGINE_RUNNER_DEFAULTS. New test tests/test_zone_gate_top_k.py (7 cases). VERIFICATION: byte-parity PROVEN — explicit baseline(worktree − my hunks) vs worktree ledger hashes IDENTICAL on BNBUSDT (bcf15081…, 10366 B) + SOLUSDT (e43e441d…, 5255 B); determinism gate 6 passed/1 xfailed; hash-NEUTRAL (_compute_hash.py hashes params block only, engine_runner untouched → no rehash); 36-test regression + behavior_census/current_findings/doc_citations 17 green. Files: configs/production/v2_multi_2026_04.json, src/core/engine_runner.py, src/engines/live_engine.py, src/engines/zone_gate_engine.py, tests/test_zone_gate_top_k.py.
Belief Update / ROI / Goal: Goal: give the zone-gate aggregation tunability without changing live behavior or claiming edge. Belief: the proposed "kWTA replacement" was largely already implemented; the real gap was config-externalization + a dead-code smell, not architecture. Knowledge ROI: medium — grants a config knob (tunability, NOT authority per §6.5 Authority Ladder; ZoneGate is an entry/selection filter, falsified by F-019…F-021/F-035, so a non-default top_k must earn ΔG001 before any production weight). Action: do NOT sweep top_k for throughput on the strength of this change; default stays 3 (byte-identical). No finding flipped.
Open Questions: Other full prod configs (v1/deepdeektry/v3/v4) lack the zone_gate block; they resolve via ENGINE_RUNNER_DEFAULTS in tests but a raw-JSON load through _build_engine_runner_config would now fail-fast — add the block if/when any is run live on this branch (v2 active per ACTIVE_VERSION).
Next Step: Commit when the user asks (on branch `patch`); optionally graduate top_k to goal-seeking only after a measured ΔG001 justification.
---
📝 SESSION LOG ENTRY
Date: 2026-06-24
Topic: Phase 9A COMPLETE — post-close swap captured; broker at Platinum (reachable); kernel untouched
Decision/Output: Held 0.01 EURUSD ~51.6h across rollovers (swap −0.02). GATE-2 RESOLVED EMPIRICALLY: MetaQuotes-Demo FOLDS swap into the close deal (OUT deal vol 0.01 + swap −0.02), NOT a separate zero-vol record. Reconstruction already correct (episode net_pnl −10.62 = gross −10.6 + swap −0.02; verify PASS 8=8 P/L diff 0.0) → ZERO kernel change. Broadened characterizer swap detector (_has_post_close_swap → _incurred_swap = any deal swap≠0, folded OR separate) + regression test test_folded_swap_counts_as_post_close_swap. Coverage now 100/100 Platinum (validated partial_closes+post_close_swaps; reachable_unseen []; n_a pyramids/inout/reopens/separate_commission). 74 tests green. MIGRATIONS updated. Closed the held position cleanly (remaining=0).
Belief Update / ROI / Goal: Goal: honest, complete broker-semantics characterization. Belief: MetaQuotes-Demo (hedging, spread-only) is now FULLY characterized — every reachable pattern observed+verified (Platinum); the unreachable ones honestly N/A. Knowledge ROI: high — the LAST reachable unknown (swap) is resolved, AND it confirmed the kernel-vs-boundary doctrine a 7th time (swap-representation was a coverage-layer concern, reconstruction was already right). The whole demo-trade reality campaign found 0 reconstruction defects across all patterns. Action: commit v0.3.1; the broker's reachable surface is exhausted → genuine resting point (further reality needs a DIFFERENT account: netting for pyramid/INOUT/reopen, commission-charging for separate_commission).
Open Questions: Other account types (netting / commission) would unlock the N/A patterns — only if/when the user connects one. Phases 9(drift)/10/11 still gated on sample size.
Next Step: commit+tag v0.3.1. Then TRUE rest — this broker is at Platinum; no further reachable evidence here.
---
📝 SESSION LOG ENTRY
Date: 2026-06-24
Topic: Framing — MT5 analytics validated the INSTRUMENT, not EDGE (calibration note)
Decision/Output: Affirmed the distinction: Platinum coverage = the measurement layer is faithful (robust reconstruction vs real broker behavior, 7 reality tests, 0 kernel defects) ≠ trading edge. Connected to repo evidence: edge is NOT an open optimistic frontier — F-019…F-035 already falsified entry/conditional/selection/exit/cross-sectional/carry edge net of costs across crypto + FX majors. MT5 analytics' real win is narrow + concrete: closes F-010 (live PnL was backtest-only/unverified) — real executions now reconstruct+reconcile, enabling "do live trades match backtested expectancy net of real costs?". Its data source IS ground truth but it measures whatever the strategy did — can't manufacture edge. Verified log rotation (rotate_session_log.py) trimmed assistant_project.md to bounded hot file (332 deletions in v0.3.1 commit) — BENIGN: spilled verbatim to docs/analysis/session-log-archive/ + preserved in git HEAD~1; recent 9A entries intact.
Belief Update / ROI / Goal: Goal: keep epistemics honest at the instrument↔edge boundary. Belief: instrument = trustworthy (high conf); edge = unproven and per existing findings largely falsified (do NOT assume). Knowledge ROI: high (prevents the #1 forward error — mistaking robust observability for validated edge). Action: any future "dashboard shows a winning pattern" claim must clear M4 (permutation/OOS/cost) + E-001 Authority Ladder (info≠value≠authority) before earning production weight. Platinum + MFE/MAE scatter = Level-1 information only.
Open Questions: none new — this is a calibration/framing entry.
Next Step: TRUE rest. Edge work, if ever, goes through M4/E-001, not through more observability. New reachable broker evidence needs a different account (netting/commission).
---
📝 SESSION LOG ENTRY
Date: 2026-06-24
Topic: Completed the docs/ uppercase→lowercase reorganization (git-staged a prior content migration)
Decision/Output: A prior session reorganized the entire docs/ tree (flat UPPERCASE → foldered lowercase: reference/architecture/analysis/governance/topics/intent/operations/plans), moved content, repointed CLAUDE.md — but never git-staged it (38 tracked HEAD vs 289 on disk; 26 D + ~250 untracked + 3 M). TRUTH-CORRECTION FIRST: my earlier "docs/ is gitignored" claim was FALSE (no docs rule in .gitignore; files were UNTRACKED, not ignored — surfaced + corrected). SAFETY GATE (didn't author these): line-normalized content audit over all 22 deletions → 0 truth lost (18 byte/line-identical in new location; 4 moved-and-edited 75-92% verbatim same-headings; first byte-exact audit's 22 false orphans were CRLF↔LF). Staged docs/ ONLY (git auto-detected 18 R100 + 3 partial renames + 1 D=TESTING.md whose new home reference/testing.md is staged as A). doc-governance gates GREEN throughout (test_doc_citations ±30 + test_topic_docs + test_current_findings = 13). Commit 151935b (reorg) + 4210a47 (recorded mt5_analytics/ READ-ONLY subsystem + manual_tools/ execution-separation in conventions.md folder-placement + architecture.md directory tree). docs/ working tree now CLEAN. Pre-existing unrelated src/config changes left untouched (docs/ pathspec only).
Belief Update / ROI / Goal: Goal: zero silent truth divergence (§6.2). Belief: the docs reorg was content-complete + CLAUDE-referenced + test-passing but unstaged — "finishing" = git hygiene, not content work. Knowledge ROI: medium — closed a long-standing repo-truth gap (tracked git now matches the canonical docs the governance layer cites) AND caught my own gitignored-vs-untracked overclaim (E-001 in action: shorter correction loop). Action: docs task done; mt5_analytics placement now canonical.
Open Questions: docs/research-readiness/*-report.json (generated data) committed as-is — a later hygiene pass could .gitignore them. The pre-existing src/config/CLAUDE.md working-tree changes (unrelated, from before this session) remain uncommitted by design.
Next Step: none required — docs/ tracked + clean + gates green. Rest.
---
📝 SESSION LOG ENTRY
Date: 2026-06-24
Topic: Formalized the Findings/Corrections discipline (fix-the-source) + corrected the "docs gitignored" overclaim at its recorded sources
Decision/Output: Extended CLAUDE.md §6.2/E-001 mandatory-phrase block with a non-optional rule: a retraction is a CORRECTION that must, the SAME turn, fix every RECORDED instance of the false claim (memory file / finding Reversal: / doc) marked `CORRECTED: <old>-><new>` + state how the true fact was verified — a chat-only retraction re-propagates. Applied it: corrected the false "docs/ is gitignored (.gitignore:13)" claim (a mis-diagnosis of UNTRACKED as IGNORED — .gitignore has no docs rule, git check-ignore clean; reorg now tracked 151935b) in 3 LIVE memory files (MEMORY.md, project_truth_audit_2026_06_11, project_session_sweep_findings). Left archived session-logs untouched (§6.2 rule 4 history). Verified: no un-CORRECTED docs-gitignored claim remains in memory (other 'gitignored' hits = data/ + results/, genuinely ignored); gates 34 passed/1 skipped (session_log/current_findings/doc_citations/epistemic_invariants). NOTE: CLAUDE.md edit left UNCOMMITTED — CLAUDE.md had pre-existing unrelated uncommitted changes, so committing would mix scopes; memory corrections persist via memory system regardless.
Belief Update / ROI / Goal: Goal: zero silent truth re-propagation (§6.2). Belief: corrections were leaking — fixed in chat but the false claim stayed live in memory and re-entered future sessions (the docs-gitignored claim survived ~2 weeks across multiple session logs). The fix-the-source rule closes that loop. Knowledge ROI: high-durability — generic mechanism (self-cleaning corrections) + the named claim retired at its sources; future overclaims now have an explicit "patch the record" step, not just an apology. Action: discipline formalized; rest.
Open Questions: CLAUDE.md correction block uncommitted (mixed with pre-existing CLAUDE.md changes) — commit only if user wants careful staging. Other historical overclaims may exist but no broad re-audit requested (scope discipline).
Next Step: none required — rule in CLAUDE.md, sources corrected, gates green.
---
📝 SESSION LOG ENTRY
Date: 2026-06-22
Topic: Phase 9A — overnight-hold position placed (swap experiment in flight)
Decision/Output: Market reopened; placed the hold via gated generator (--patterns hold): BUY 0.01 EURUSD retcode 10009, position ticket 9189938051 @ 1.14588, LEFT OPEN. Confirmed live: swap_so_far=0.0 (pre-rollover), AutoTrading on, UTC now 2026-06-22 05:28. Swap accrues only at the broker daily rollover (MetaQuotes server=UTC+3 → ~21:00 UTC); position must be held ACROSS that rollover. Closing now = zero swap, so HOLDING.
GATE-2 PRE-NOTE (important): my characterizer `_has_post_close_swap` requires a SEPARATE zero-volume swap deal. If MetaQuotes FOLDS swap into the close deal (volume>0 with swap!=0), reconstruction still sums it correctly (net_pnl right, verify PASS) but post_close_swaps would NOT flip to observed → that's a CHARACTERIZER broadening (count swap on any closed-position deal), NOT a kernel change. Decide empirically by inspecting the close deal shape after closing.
Belief Update / ROI / Goal: Goal: capture the last reachable pattern (post_close_swap) → hedging broker Silver 50 → Platinum. Belief: actionable step done; remainder is broker-rollover-time-gated (~16h). Knowledge ROI: pending (the deal-shape observation is the payload). Action: after ~21:00 UTC rollover today, run `--patterns close` → coverage → verify; inspect whether swap is a separate deal or folded; refine characterizer only if folded.
Open Questions: separate swap deal vs folded-into-close? (the Gate-2 empirical question). Triple-swap day? (rollover size varies; irrelevant to detection).
Next Step: hold across the ~21:00 UTC rollover → close (ticket 9189938051) → coverage/verify → characterize swap representation.
---
📝 SESSION LOG ENTRY
Date: 2026-06-21
Topic: Frozen-sentence TruthConflict RESOLVED + optional honest self-report Metrics Block shipped
Decision/Output: Workstream A — propagated CLAUDE.md §6.1's fuller frozen sentence (goal-probability clause) to docs/architecture/intelligence-compounding.md:11-14, closing the §6.1↔long-form DOC_DRIFT (no test pins the text); marked the panda report RESOLVED (append-discipline, §6.2 rule 4). Workstream B — defined the 7-field Metrics Block (GP|Act|Rec|Find|Ent|NS|KROI) canonically in intelligence-compounding.md with a top guardrail callout (self-report, ZERO authority, never settles a TruthConflict — §6.5/E-001), annotated each field's epistemic tier (Find/Ent/NS have measured proxies; GP/Act/Rec/KROI self-report); thin OPTIONAL pointer in CLAUDE.md §7.4 (not a §6 mandate, not enforced by test_session_log.py:11); built read-only prospective extractor scripts/metrics/extract_metrics.py (pure parse_metrics + safe_print CLI) + tests/test_extract_metrics.py (5 green). Pre-registered facts: the 7 metrics were NOT a prior canonical artifact and no historical log contains a block (extractor is prospective by design).
Belief Update / ROI / Goal: Goal: resolve the doc conflict at proportionate cost while enabling honest operational telemetry. Belief: tabulating self-rated grades is compliance telemetry, not empirical evidence — so the conflict was settled by a 1-line edit (user choice), never by the metrics (§6.5 ladder, E-001). Knowledge ROI: high — separated a cheap doc fix from an over-scoped "empirical" instrument and froze the zero-authority guardrail into canon. Action: ran prescribed rotation to clear the PRE-EXISTING session-log backlog (45>30, untouched by this work).
Open Questions: Should the Metrics Block ever become mandatory (would require a test_session_log.py change)? Deferred — optional in v1. B4 reality cross-check (self-report vs proxy divergence) deferred.
Next Step: Adopt the block in future entries to begin prospective tracking; revisit B4 if/when self-report diverges from the measured proxies.
---
📝 SESSION LOG ENTRY
Date: 2026-06-21
Topic: B4 (Metrics Block reality cross-check) explored and DEFERRED with a tightened reopen contract
Decision/Output: Explored B4 and confirmed deferral (both user and Claude agree). Sharpened the field-by-field feasibility with one structural correction: the extractor's ONLY input is the self-authored assistant_project.md, so a cross-check is only as "real" as its EXTERNAL data source. Result — Ent = genuinely viable (MEMORY.md byte-size vs limit, external + cheap); Find = feasible but heavier (must diff docs/current-findings.md, NOT test_current_findings.py which only checks the findings doc's mechanical contract); NS = wrong architecture for a log parser (references_frozen_sentence(log_text) is circular — a true NS check must run in-session, separate design); GP/Act/Rec/KROI = pure self-report. Recorded sharpened analysis + reopen conditions in the plan file (Ent-first, external-source-only, calibration-not-judgment header, NS handled separately, reopen at ~10–15 real blocks). No code shipped this turn.
Belief Update / ROI / Goal: Goal: keep the self-report Metrics Block honest over time without premature framework. Belief: a reality cross-check is only meaningful against an external source, which collapses the viable surface to essentially one cheap proxy (Ent) — so deferring until real usage data exists is correct, not just cautious. Knowledge ROI: high — clarified the log-vs-live-turn boundary that governs ANY future B4 architecture and prevented building a circular NS check. Action: hold B4 deferred; revisit only when reopen conditions are met.
Open Questions: Whether NS calibration is worth an in-session self-check at all (separate from the extractor) — not opened.
Next Step: Adopt the Metrics Block in future entries to accumulate real data; revisit B4 (Ent-first) once ~10–15 blocks exist.
---
📝 SESSION LOG ENTRY
Date: 2026-06-19 00:10
Topic: F-035 — FX cross-asset M4 run (entry-info null generalizes beyond crypto) + MT5 fetch layer built
Decision/Output: Unblocked the data-gated FX/metals frontier. AV free tier = FX_INTRADAY premium-only (1-request probe proved it); MT5 chosen (free). MetaTrader5 cp314 wheel installs; built dedicated MT5→CSV fetcher (src/inout/mt5_candle_fetcher.py + scripts/data/fetch_candles_mt5.py — reuses historical_fetcher._fetch_from_mt5 conversion, native UTC M15, symbol_info.digits rounding; historical_fetcher itself is DB-centric, no MT5→CSV writer) + wired AV key to env `fetch_candles_alphavantage_apikey` via stdlib _load_dotenv. Fetched 2yr M15 ×6; 5 FX (EURUSD/AUDUSD/EURCAD/GBPUSD/USDJPY) APPROVE/WARN 0.00% missing (~49.5k each); XAUUSD REJECT (26h holiday gap > FX gap gate = Stage-2 metals calendar) → excluded; originals backed up data/_archive_5wk/. Built qualify_fx_metals.py + research_config_fx_metals.json (+spine) cloned from *_majors. RESULT F-035 (VALIDATED/Likely): 0 PROMOTE. Toys WELL-POWERED REJECT (expansion_breakout n=7.7–8.9k PF 0.02–0.08 E −1.7…−2.8R; mean_reversion n=13.7–17.1k PF 0.02–0.10 p=0.0005 NEGATIVE); spine INSUFFICIENT per-inst 2–8 trades (corroborates F-029), POOLED n=30 REJECT. Determinism byte-identical sha 48a0193c. Registered F-035 in docs/current-findings.md + CLAUDE.md Repo Truths Index (test_current_findings 8 green). Memory project_fx_cross_asset_f035 + index.
Belief Update / ROI / Goal: Goal: test whether the crypto entry-info null is asset-class-specific. Belief: it is NOT — the null generalizes to FX majors (first non-crypto falsification, extends F-019…F-032). Knowledge ROI: high — closes the "maybe FX carries the edge crypto lacks" escape hatch with a powered result, and the MT5 fetch layer + 5-FX 2yr corpus are permanent reusable assets. CAVEAT preserved (E-001): 12bps cost-dominates FX magnitude (=1.8–2.5× median bar, extends F-025) but PF≪1 makes the direction null cost-robust; no realistic-cost rerun needed (not a near-miss). Action: FX directional frontier closed; metals (XAU) pending Stage-2 holiday calendar; new files untracked → commit when ready.
Open Questions: XAUUSD/metals — add Stage-2 holiday-calendar handling (or one-off gap exception) to run gold? Commit the new fetch+qualify layer now or batch?
Next Step: Owner: decide commit grouping + whether to pursue metals (needs calendar work) vs treat the FX cross-asset question as closed.
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: M7 — Workflow tab audit: the hand-drawn DAG eliminated the whole "Replay & Backtest" stage; made it faithful + whole-system + guarded
Decision/Output: AUDIT FINDING: the Workflow-tab Mermaid (hand-drawn string) had drifted from the codebase's own registry.WORKFLOW_STAGE_ORDER — it eliminated the ENTIRE "Replay & Backtest" stage (no backtest.v2/bitnet/replay node, despite the Header breadcrumb listing it + it being the most-run workflow), showed validation.config_validator only as inline text, and mis-ordered Tuning->Promotion (skipping Model Training + Backtest). Owner decisions: faithful hand-fix + coverage guard; whole-system scope. FIX: (1) flow_context/telemetry.json — new S7 flow manifest (events.event_fabric + utils.engine_telemetry/integrity_events/episode_summarizer + core.collector; doc event-taxonomy.md), closing the long-open telemetry-flow gap; flow_graphs/telemetry.dot regenerated (35 edges, byte-identical). (2) WorkflowPanel.jsx rewritten faithful to WORKFLOW_STAGE_ORDER: added CV(validation.config_validator) + BT(backtest.v2, between Validation and Live) nodes, corrected the tuning->validate->promote chain, added an orthogonal side-subsystems subgraph RS/AG/TE; NODE_TO_CMD += CV/BT (command nodes -> M5 drawer), NODE_TO_FLOW={Research:research, AI Agent:agent, Telemetry:telemetry} (flow nodes -> M6 Explorer); delegated click routes command->onNode else flow->onFlow. (3) App.jsx onFlow=(f)=>{setExploreFlow(f);setView('explore')} + ExplorerPanel initialFlow auto-select (deep-link from Workflow side node to Explore). (4) tests/test_workflow_dag_coverage.py — parses WorkflowPanel NODE_TO_CMD, asserts every WORKFLOW_STAGE_ORDER stage has >=1 represented command; red->green proven (removing backtest.v2 -> guard reports "Replay & Backtest" missing). All 6 stages now represented incl. Replay & Backtest. 58 tests green. No src/ or configs/ change.
Belief Update / ROI / Goal: Goal: make the Workflow tab faithfully reflect codebase logic (no silently-eliminated flow). Belief: the hand-drawn DAG was an unguarded duplicate of registry truth and had silently dropped the most-used stage — exactly the documentation-entropy failure mode §6.2 warns about; the fix + the coverage guard convert it from prose-cartoon to test-enforced. The owner's "derive/guard, never hand-maintain-unchecked" value applied again. Knowledge ROI: high (caught a real, user-visible omission of the backtest workflow). Action: optional principled follow-on = derive the whole Mermaid from WORKFLOW_STAGE_ORDER + recommended_next so it's drift-proof by construction. JSX needs a browser load. Commit M6+M7 together or M7 separately.
Open Questions: ExplorerPanel/WorkflowPanel/App JSX not compile-checked here (Babel-in-browser) — needs a hard-refresh load. Whether to derive the DAG fully (principled follow-on). Commit grouping for the uncommitted M6+M7.
Next Step: Owner: hard-refresh -> Workflow tab -> confirm Replay & Backtest (backtest.v2) node present + config_validator node + Research/Agent/Telemetry side nodes (clicking a side node jumps to Explore on that flow). Then commit M6+M7. Backtest now a clickable runtime-flow node (M5 drawer).
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics subsystem — Phase 1 (foundation) + Phase 2/2a/2b (reconstruction kernel) shipped & green
Decision/Output: Built top-level `mt5_analytics/` (separate, disposable intelligence layer beside Tradelatest; MT5 owns truth, Python owns intelligence). KERNEL-FIRST per approved plan. Files: schemas/position_episode_v1_0.py (PositionEpisode — logical position_id vs unique analytical episode_id; make_episode_id deterministic), engines/position_reconstructor.py (deals→episodes; handles partial close, pyramiding/VWAP, multiple tickets/one position_id, separate+post-close commission/swap, INOUT reversal close-old+open-new, reopen-after-close, completion guard), core/mt5_adapter.py (READ-ONLY: history_deals_get/history_orders_get/positions_get/account_info/copy_rates_range, import-guard mirrors mt5_candle_fetcher, NO mutation API), core/checkpoint.py (O(new) cursor), storage/partition_writer.py (date-partitioned JSONL, dedup on episode_id, reuses utils.jsonl_writer), storage/manifest_builder.py (sha256 order-independent + provenance: source_history_window/engine_registry_hash/rebuild_id/generated_by/python_version), registry/engine_registry.py. Wired pyproject pythonpath += "." + optional-deps group; .gitignore for artifacts/reports/state/audit. Phase 2a torture suite: tests/mt5_analytics/ 20 tests GREEN (0.25s). Fixed pytest path-clash (removed tests/mt5_analytics/__init__.py — was shadowing the real package). Full-suite collection clean (1992 tests, no import errors).
Belief Update / ROI / Goal: Goal: give the owner trustworthy post-trade analytics on REAL MT5 executions (vs backtest-only F-010). Belief: the sacred kernel (position reconstruction) is the only place truth can be corrupted; everything above is a disposable plugin. Knowledge ROI: high — reconstruction now provably handles all 8 broker edge cases under deterministic fixtures, no live terminal needed (synthetic-fixture strategy validated). Action: kernel gate satisfied → Phase 3 (intelligence: MFE/MAE via reused horizon_excursion, RR, duration/session/regime) may now proceed.
Open Questions: Live-terminal smoke (real deals) still pending a connected MT5. Phases 3–11 (intelligence/shared_pipeline/rebuild+daemon/verify/audit/storage/dashboard/research) not yet built.
Next Step: Phase 3 position intelligence — map PositionEpisode→research Signal, wrap horizon_excursion (src/research/measurement/forward_walk.py) for MFE/MAE, rr_engine, metrics_oracle expectancy; then Phase 4 shared_pipeline core.
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics reconstruction kernel — critical self-review
Decision/Output: Line-level review of position_reconstructor.py + mt5_adapter.py. Verdict: correct for all 8 torture scenarios, NO correctness bug for realistic MT5 data. 3 low-severity findings: (1) open_position_ids param is inert inside _segment_position — guard is purely volume-balance (correct, but docstring oversells); (2) cashflow-BEFORE-entry ordering mis-seeds placeholder leg's entry_time/direction (rare: brokers emit commission AFTER trade; cheap fix = reset entry_time_raw/direction when IN lands on empty-in leg); (3) INOUT deal ticket appears in BOTH episodes' deal_tickets (defensible, needs doc note). Untested surfaces: adapter/checkpoint/partition never run vs live MT5; positions_get 'identifier' field assumption unverified; OUT-never-exceeds-open assumption undocumented.
Belief Update / ROI / Goal: Goal: trustworthy reconstruction (the sacred kernel). Belief: kernel is sound; the only real risk is the cashflow-before-entry ordering, which is in-kernel so cheap hardening is high-ROI despite low probability. Knowledge ROI: medium — confirmed no realistic-data bug, narrowed residual risk to one rare ordering + live-surface unknowns. Action: offered ~10-line hardening (Findings 2/3 + new fixture) vs proceed to Phase 3; awaiting user direction.
Open Questions: Apply hardening now or proceed to Phase 3? Live-terminal smoke still pending.
Next Step: On user OK — either kernel hardening pass, or Phase 3 (PositionEpisode→horizon_excursion MFE/MAE, rr_engine, metrics_oracle expectancy).
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics Phase 3 (Kernel 2 — feature generation) shipped & green
Decision/Output: Built the PositionEpisode→FeatureRecord layer (the SECOND sacred boundary) in the approved order: engines/features/_bars.py (Bar value object + build_window re-index/entry-locate), rr_engine.py (per-episode realized R = (exit-entry)·dir/risk; risk=|entry-SL| else ATR fallback; NO aggregation), mfe_mae_engine.py (reuses horizon_excursion via the adapter trick atr=risk_distance/sl_atr_mult=1.0 → mfe_r/mae_r in trade's OWN R), duration_metrics.py, session_metrics.py (London/NY/Asia/OVERLAP/OFF from _require'd config bands), regime_engine.py (RegimeLabeler; C/N/E vs None=insufficient vs UNKNOWN=raised), engines/entry_tracker.py (path peak/trough + bars-to-peak + went_adverse_first), feature_record_builder.py (SINGLE stamping/merge point; guards reserved-key collisions AND forbidden aggregate keys = purity invariant), feature_engine.py (pure orchestrator, never touches MT5/files/aggregation). + analytics_config.py (strict loader, no silent defaults) + config/analytics.json + schemas/feature_v1_0.py. Deferred as planned: gaussian_engine + crt_validator (raw MT5 trades carry no CRT/zone features). Tests: tests/mt5_analytics/ now 32/32 green (20 kernel + 12 Phase 3): MFE/MAE exact parity (3R/-0.5R), realized-R long/short, ATR fallback, duration, session OVERLAP, regime None-vs-UNKNOWN, purity (no aggregate keys + builder rejects collisions/aggregates), determinism, build_window.
Belief Update / ROI / Goal: Goal: trustworthy per-trade intelligence on real MT5 executions. Belief: feature generation is a second kernel deserving reconstruction-grade rigor; the horizon_excursion reuse means MFE/MAE in true-R cost ~zero new math. Knowledge ROI: high — purity invariant is now test-enforced (builder structurally rejects aggregate leakage), so the most-likely Phase-3 failure mode (stats creeping into features) is closed. Action: Phase 3 core done → Phase 4 (shared_pipeline) next, where deferred kernel findings K-1/K-2/K-3 get batched.
Open Questions: Live-terminal smoke still pending. CandleProvider impls (CSV/MT5) to be wired in Phase 4 shared_pipeline. gaussian/crt_validator still deferred (need a feature source).
Next Step: Phase 4 — core/shared_pipeline.process_closed_positions() (rebuild↔daemon anti-divergence core) + CandleProvider protocol + wire deferred kernel findings K-1/K-2/K-3.
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics Phase 4 (Kernel 3 — shared pipeline / integration boundary) shipped & green
Decision/Output: Built the THIRD sacred boundary: the single pipeline path both entry points terminate in. core/shared_pipeline.process_closed_positions() (reconstruct→SL-lookup+CandleProvider.get_window+build_window→feature_engine.compute→PartitionWriter episodes+features; everything INJECTED) returning typed RunSummary (schemas/run_summary_v1_0.py: episodes_seen/written, features_written, duplicates_skipped, episodes_skipped, anomaly/warning counts, duration_ms, pipeline_version=registry_hash; to_audit_dict() = Phase-6.5 payload). providers/: CandleProvider Protocol + FixtureProvider (in-memory, test) + MT5CandleProvider (copy_rates_range→Bar, lazy mt5). Thin shells core/rebuild.py (argparse, full-range, never auto-runs) + core/daemon.py (tick() testable unit + run() loop, O(new) via checkpoint, open_position_ids cross-check) + core/triggers.py. BATCHED deferred kernel findings (last kernel-touch): K-1 (open_position_ids now honest cross-check + DEBUG anomaly on balanced-leg-but-open; docstring de-oversold), K-2 (cashflow-before-entry: IN on empty leg resets entry_time_raw+direction; torture test added), K-3 (deal_tickets-not-a-partition doc note on module + schema field). Tests: tests/mt5_analytics/ now 42/42 (was 32; +K-2, +4 providers, +3 shared_pipeline, +KEYSTONE test_pipeline_parity, +entrypoint import). Repo-wide collection clean (2014).
Belief Update / ROI / Goal: Goal: one coherent, replayable MT5-analytics pipeline. Belief: the anti-divergence property is now TEST-ENFORCED — rebuild (1 batch call) and daemon (N incremental ticks) produce byte-identical episodes.jsonl+features.jsonl (test_pipeline_parity). Knowledge ROI: high — the worst long-term failure mode (rebuild≠daemon → two truths) is structurally closed; rebuild/daemon are now thin shells over one core. Action: structural phases done (Kernels 1/2/3); architecture enters stabilization. Remaining = capability plugins (verify/audit/storage/dashboard/research), all deferred + lower-confidence.
Open Questions: Live-terminal smoke (rebuild/daemon vs real MT5) still pending. gaussian_engine/crt_validator/analytics/dashboard/drift/clustering/montecarlo deferred. Audit emission (Phase 6.5) persists RunSummary.to_audit_dict() — not yet wired.
Next Step: Phase 6 verify.py (reconcile MT5↔artifacts) + Phase 6.5 audit chain (persist RunSummary), OR live smoke once a terminal is connected. No more structural redesign expected.
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics Phase 5.5 — Live Terminal Smoke (the reality bridge) shipped
Decision/Output: Built tests/manual/live_smoke.py — a standalone manual harness (NO test_ prefix → never CI-collected; verified `pytest tests/manual/` collects 0) running 7 reality checks against a real MT5 terminal: (1) real closed position reconstructs, (2) idempotency (tick×3 no dups), (3) live replay parity rebuild==daemon by sha256(episodes/features), (4) manifest provenance keys, (5) no balance/equity/margin/login/server persisted (account_info()+gc.collect()+scan), (6) execution unreachable (MT5Adapter has no order_send/position_* attrs — structural, runs without terminal), (7) MT5CandleProvider returns real bars. Writes ONLY to tempfile.mkdtemp() roots (cfg artifact_root override + daemon._CHECKPOINT_PATH monkeypatch) — never repo artifacts. No terminal/MetaTrader5 → clean SKIP, exit 0. Reuses safe_print/read_jsonl/load_config/rebuild.run/daemon.tick. + CI-safe anti-bitrot guard tests/mt5_analytics/test_live_smoke_importable.py (imports by path, asserts check fns exist + structural check runs). Suite now 43/43.
FIRST REAL RUN: MetaTrader5 IS installed + a terminal initialized successfully on this machine (mt5.initialize() OK), but the connected account has 0 closed positions in last 30d → result 3 PASS / 0 FAIL / 4 SKIP, exit 0 (execution_unreachable PASS, replay_parity PASS [both empty→equal], no_account_state PASS; data-dependent checks SKIP honestly). Confirms the live plumbing connects; awaits an account WITH trade history to exercise checks 1-4,7 fully.
Belief Update / ROI / Goal: Goal: validate the frozen architecture against reality, not more design. Belief: live plumbing WORKS (terminal connects, rebuild/daemon run end-to-end against real MT5) — but the account under test has no recent closed trades, so reconstruction/feature checks are unexercised on real data. Knowledge ROI: medium-high — de-risked the live connection + proved graceful SKIP/degradation + no-CI-contamination; the remaining unknown narrows to "real deal/commission/swap semantics" which needs an account with history. Action: architecture stays frozen; to fully exercise 5.5, point at an account with closed positions (or widen DAYS). Then Phase 6 verify + 6.5 audit.
Open Questions: Need an MT5 account WITH closed-trade history (or a larger DAYS window) to exercise live checks 1/2/3(non-trivial)/4/7. Phase 6/6.5 still pending.
Next Step: Either (a) re-run live_smoke against an account with trade history (bump DAYS or switch account), or (b) proceed to Phase 6 verify.py + 6.5 audit (persist RunSummary.to_audit_dict()).
---
📝 SESSION LOG ENTRY
Date: 2026-06-19
Topic: mt5_analytics Phase 6 (verify) + 6.5 (audit) shipped — incl. a REALITY-FOUND bug fix
Decision/Output: core/verify.py: reconcile(deals, artifact_root) pure core (round-trip integrity: missing/orphan/duplicate episode_ids; INDEPENDENT P/L oracle = Σ raw-deal cashflow per position_id vs Σ artifact net_pnl; volume round-trip; manifest sha256 + provenance recheck; corrupt-line→errors.jsonl) + live run(from,to) wrapper (writes reports/verification/daily_verification.md + audit line) + CLI. schemas/verification_report_v1_0.py (VerificationReport + to_audit_dict + to_markdown). core/audit.py append_audit(kind,payload) → audit/{rebuild_runs,daemon_events,verification_runs}.jsonl (append-only); wired additively into rebuild.run + daemon.tick. config += audit_root. Tests: test_verify.py (zero-trade PASS, happy PASS, 4 corruption levers each→FAIL independently, markdown) + test_audit.py. Suite 43→53.
REALITY-FOUND BUG (the live run earned its keep): live verify on the "empty" demo account returned FAIL net_pnl_diff=100000. Root cause via live inspection: history_deals_get returns the initial DEPOSIT deal (position_id=0, type=2=DEAL_TYPE_BALANCE, volume=0, profit=100000=demo balance). The P/L oracle naively summed it. FIX: added is_position_deal(deal) = position_id != 0 (account ops — deposits/credits/bonuses — carry pid 0; per-position commission/swap deals reference their pid so are KEPT), applied in BOTH reconstruct_position_episodes (upfront filter) and verify's oracle. Regression test test_balance_deposit_deal_ignored. Live verify now PASS (net_pnl_diff 0). Fixtures could NOT have caught this — exactly the Phase-5.5 thesis.
Belief Update / ROI / Goal: Goal: a verifiable, self-auditing MT5 analytics pipeline. Belief: confirmed the reality principle empirically — a real broker emits non-trade account deals that synthetic fixtures never modeled; verify is now hardened against them. Knowledge ROI: HIGH — one live run surfaced + fixed a P/L-corruption-class bug AND established the position_id!=0 trade-deal boundary (will matter for every future P/L path). Knowledge ROI of the empty account = high (a "null result with a clear conclusion"). Action: pipeline is now structurally + operationally complete (ingest→reconstruct→featurize→persist→verify→audit), all green on real infra. Next real gain = a rich-history account (Phase 5.5 round 2).
Open Questions: Still need an account WITH closed trades to exercise reconstruction vs real broker semantics (partial/pyramid/commission-as-typed-deal/INOUT). Are there MORE non-trade deal types to filter (CREDIT/CHARGE/CORRECTION) — position_id!=0 should cover all, but confirm on a funded account.
Next Step: (a) point at an account with trade history and re-run live_smoke + verify, or (b) optional plugins (Phase 7 parquet/duckdb · 8 dashboard · 9-11 research). No structural work remains.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-25 23:25 IST
Topic: F-038 — the gate-ON fusion "RR" engine is a GAUSSIAN DUPLICATE (rr_fusion degrades to gaussian); corrects an external r=0.005 claim
Decision/Output: Investigated the gaussian≡rr side-flag from F-036/gate-ON. An LLM-advisor asserted r=0.005 / means 0.99998·0.7783 / "logging artifact". WENT TO THE DATA (logs/signal_audit.jsonl from the gate-ON Phase-2 run): rr_score==gaussian_score EXACTLY per-bar (r=1.0), NOT r≈0. Code-traced: config_layer/rr/rr_fusion.py _passthrough() returns final_score=gaussian_score; engine_runner.py:694-699 adopts it as rr_result.score; rr_fusion.enabled=true → base RREngine score (self.rr.compute) DISCARDED. Instrumented probes (FusionEngine.compute + RRFusionLayer.score_dict counters) over full gate-ON BNB/ETH/BTC/SOL: rr==gaussian 26/26 fusion bars (BNB11/ETH4/BTC5/SOL6), via bypassed_low_confidence 23/26 (rr_fusion NanoInference model confidence < confidence_bypass_threshold 0.3 → returns gaussian) + drift_detected 3/26 (disp_strength > drift_threshold 1.5). So the "4-engine" fusion has ≤2.5 INDEPENDENT signals; gaussian double-weighted (gaussian+rr slots), against CRT 0.4 + zone 0.2. Filed F-038 (ARCH/Likely; docs/current-findings.md + CLAUDE.md index, test_current_findings + test_doc_citations 10 green). Compounds F-036 (WHY zone non-pivotal: fused = CRT + doubled near-constant gaussian ~0.883 + zone). INTENDED-vs-DEFECT surfaced as OPEN (not resolved, §6.2 rule 3): rr_fusion is documented "advisory" graceful-degradation (passthrough by-design) BUT fires ~100% (model never contributes) AND enabling it silently discards the base RR engine.
Belief Update / ROI / Goal: Goal: faithful picture of the fusion stack (for any future fusion-based work). Belief CORRECTED (mine + advisor's): gaussian and rr are NOT independent — rr is a gaussian duplicate via rr_fusion degradation; the advisor's r=0.005 was wrong (didn't go to data). Knowledge ROI: high — "4-engine fusion" is a misnomer (≤2.5 independent signals); this is load-bearing for interpreting fusion authority/weights and explains the zone (and rr) non-pivotality structurally. Action: do NOT treat rr as an independent edge in fusion work; the open question (rr_fusion model never confident + base RR discarded) is a real candidate defect to raise with the owner. Reaffirms the doctrine: GO TO THE DATA, don't accept an advisor's correlation claim (E-001 / §13.8 advice-is-not-authority).
Open Questions: (1) INTENDED graceful-degradation vs DEFECT (rr_fusion model never confident; base RR silently discarded when enabled)? owner call. (2) Is the rr_fusion model mis-calibrated (confidence always < 0.3) or is 0.3 mis-set? (3) gaussian near-constant ~0.883 — separate non-discrimination question, own evidence. (4) live-path (vs gate-ON backtest proxy) unverified.
Next Step: Surface intended-vs-defect to owner. If defect: either disable rr_fusion (restore base RR) or fix the model/threshold; either way re-measure fusion independence. Untracked probe artifacts under results/research/_rr*.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-25 23:40 IST
Topic: F-038 root cause CONFIRMED = DEFECT — rr_fusion model is feature-STARVED (gets 2-3 of 38 expected features) → 100% bypass
Decision/Output: Per owner "investigate as likely defect, read-only first." Instrumented NanoInferenceEngine.predict (BNB gate-ON): model len(W)=38, zero_indices=11 (→ ~27 real features expected), but inference feeds only 2-3 NONZERO features (retest_depth/body_ratio/disp_strength via rr_fusion.score_dict→_empty_canonical_features). The ~24 artificially-zeroed features sit far outside training distribution → Mahalanobis d_sq saturates (_MAHAL_CLIP=500) → confidence=exp(-0.5·d_sq)≈1e-88…1e-66 ≪ confidence_bypass_threshold 0.3 → bypass on 100% of bars (9/9). So the model is STARVED of inputs, NOT genuinely unconfident — the "advisory graceful-degradation" masks a feature-interface bug: engine_runner passes 3 scalars to rr_fusion, the model needs the full canonical vector the other engines already get from input_data. Updated F-038 (root cause = DEFECT confirmed; doc + index; test_current_findings + test_doc_citations 10 green). Fix is a CODE change (not read-only) → STOPPED for owner go-ahead.
Belief Update / ROI / Goal: Goal: faithful fusion + decide the rr_fusion defect. Belief: F-038 intended-vs-defect RESOLVED → DEFECT (feature starvation, not design). Knowledge ROI: high — a concrete, fixable interface bug that silently removes a whole engine from the fusion; found by going to the data + instrumenting predict, not by accepting the "advisory degradation is intended" story. Action: present two fix options (feed full vector vs disable rr_fusion→restore base RR) + the caveat that base-RR independence/usefulness is itself unverified; do NOT change live fusion behavior without owner approval + parity/validation (§6.5).
Open Questions: (1) Fix A (feed full 38-dim vector to rr_fusion) vs Fix B (disable rr_fusion, restore base RREngine)? (2) Is the base RREngine score actually independent+useful, or also ≈gaussian/constant? (unverified — check before Fix B). (3) Even fed real features, is the rr_fusion model any good? (4) live-path unverified.
Next Step: Owner picks fix path (or park). Whichever: re-measure fusion independence (rr vs gaussian) after the change; byte-parity discipline; this is gate-ON/live behavior so it's a governed change, not a research-only tweak.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-25
Topic: Verified the backtest OHLCV file-read trace vs source; closed the residual 38-dim DOC_DRIFT (code comment + 2 living docs the 2026-06-11 pass missed) + a non-existent FeaturePipeline.transform citation.
Decision/Output: Ground-truth corrections — (1) src/features/feature_schema.py:45 comment "= 32" -> "= 38"; (2) docs/architecture/signal-flow.md Step 1 (named real reader CandleLoader.stream() + two-tier integrity: always-on inline L1/L2 backstop vs optional dataset_integrity.validate_dataset pre-flight via MultiInstrumentRunner) and Step 2 (FeaturePipeline.transform(candle,history) -> .run() batch; 35-dim -> 38-dim); (3) docs/reference/schemas.md §2.1 Candle defaults (volume=0.0, index=0) to match crt_engine_v2.py:94; (4) docs/reference/conventions.md:44 + testing.md:107 35->38; (5) docs/topics/feature-schema.md Discussion FULLY-CLOSED entry + Updated: bump. Gates: test_doc_citations + test_topic_docs 5 passed; feature_schema imports (dim=38); grep guards clean. Narrative's biggest errors (Candle "in data_ingestion/ohlcv_schema"; backtest "delegates CSV read to data_ingestion"; feature pipeline "per-bar streaming") were corrected against source — reader is CandleLoader IN backtest_v2.py; ohlcv_schema supplies only per-field validation helpers; FeaturePipeline.run() is batch.
Belief Update / ROI / Goal:
  Goal: zero silent truth divergence on the OHLCV ingestion contract (§6.2).
  Belief: the canonical OHLCV read path is CandleLoader.stream() (runtime) -> Candle (crt_engine_v2) -> batch FeaturePipeline.run() (38-dim) -> EngineRunner; integrity is two-tier (always-on inline backstop + optional pre-flight gate), NOT a single data_ingestion delegation.
  Knowledge ROI: high — DOC_DRIFT, not a new F-id; closed a drift the prior pass explicitly deferred, and corrected a doc citing a method that does not exist (would have failed a future reader/audit).
  Action: trace now matches source; future ingestion debugging starts from CandleLoader.stream() + the two-tier integrity model.
Open Questions: docs/architecture/pipeline-linkage-spine-as-hypothesis.md:32,53 still say "35-dim" but were intentionally left as a dated "shipped 2026-06-11" snapshot (§6.2 rule-4 history); revisit only if it is reclassified as a living doc.
Next Step: none required; optionally fold the corrected ingestion narrative into docs/topics/feature-schema.md's plain-language section if a fuller writeup is wanted.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-25 23:48 IST
Topic: F-038 fix PARKED by owner
Decision/Output: Owner chose "Park — record and stop." F-038 (RR=gaussian duplicate via rr_fusion feature-starvation) + confirmed root cause are filed in docs/current-findings.md + CLAUDE.md index (10 green). No code change made; rr_fusion left as-is. Fix paths preserved for a future dedicated session: (A) feed rr_fusion the full 38-dim canonical vector; (B) disable rr_fusion to restore the base RREngine — gated on first verifying base-RR independence (read-only). Untracked artifacts: scripts/research/diagnose_zone_inertness.py + results/research/{zone_inertness,zone_inertness_gateon,_gate_impact,_rr*}/ (results gitignored). Nothing committed (owner hasn't asked).
Belief Update / ROI / Goal: none (process/park entry). Goal: respect the stop; preserve the thread for clean resumption. Action: await owner; offer commit on request.
Open Questions: (carried) base-RR independence; rr_fusion model quality if fed real features; live-path verification. All deferred.
Next Step: Stop. On request: implement Fix A/B (governed change + parity/validation) or commit the scripts + finding edits.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: stream() "more than CSV parsing" doc + new finding F-039 (L3 dataset-integrity pre-flight is backtest_v2-only); conceded two review corrections on the no-lookahead explanation.
Decision/Output: (1) Conceded review: no-lookahead is the CONJUNCTION of L1 schema + L2 temporal + L3 dataset-intel + RT generator causal-ordering (not one gate); volume requirement flows schema->columns, not loader->volume. (2) Static call-site census (grep CandleLoader/validate_dataset across src/) resolved the open "does every caller run L3?" -> NO: validate_dataset is wired ONLY into backtest_v2 (_preflight_dataset:2769 + validate_universe:2586); all other stream() consumers (entire src/research/ qualification pipeline, sl_tp_comparator, portfolio_validation, exit_model_band, unified_replay_harness, config_validator) rely solely on the always-on inline L1/L2 backstop. (3) Registered finding F-039 (ARCH/VALIDATED/Certain) in docs/current-findings.md + CLAUDE.md §6.2 index (pair test-enforced), with E-001 scope guard: does NOT invalidate F-019..F-035 (inline backstop DID enforce schema+chronology) — flags a single-layer fragility. (4) Fixed DOC_DRIFT: dataset_integrity.py:24 docstring "every backtest entry point calls" -> real scope. (5) Added "Why stream() is more than CSV parsing" section to docs/topics/feature-schema.md (double-entry-bookkeeping analogy, 4-layer model, generator-as-causal-constraint, schema->columns) + L3-coverage table to signal-flow.md Step 1. GATES: test_current_findings + test_topic_docs + test_doc_citations PASS (13); test_epistemic_invariants 31 passed/1 skipped/1 FAILED — the fail is PRE-EXISTING F-038 (prior uncommitted session; evidence cites gitignored results/research/_rr_probe2), NOT F-039 (Certain, evidence resolved). Grep guard clean (no "every backtest entry point" in src/). DEVIATION: skipped the planned timeline.md F-039 row — the dated log is already stale at 2026-06-13 (F-029..F-038 absent), so a lone 06-26 row above a 13-day gap would mislead; backfill is out of scope.
Belief Update / ROI / Goal:
  Goal: zero silent truth divergence on the ingestion integrity model (§6.2) + compound the reviewer's sharpening.
  Belief: the no-lookahead guarantee is layered (L1/L2/L3+generator); on the research corpus the INLINE L1/L2 backstop is the SOLE integrity net (L3 = backtest_v2 only). The inline backstop is load-bearing, not redundant.
  Knowledge ROI: high — resolved a standing unknown with an exhaustive census, corrected a real docstring overclaim, and registered the architecture as F-039 so future sessions can't silently assume universal L3. Also surfaced a pre-existing CI red (F-038 evidence).
  Action: documented the asymmetry; do NOT add L3 to research paths without separate evidence-gating (Authority Ladder: information->doc/research authority only).
Open Questions: (a) F-038 evidence-resolvability is red (prior session) — re-point to a committed artifact or commit the probe output? (b) timeline.md is stale 06-14..06-25 — backfill F-029..F-039 as a separate pass? (c) MEMORY.md is over its size budget — F-039 left in the findings ledger (authoritative) rather than added to the over-budget index.
Next Step: await user steer on F-038 red + timeline backfill; otherwise task complete.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Reviewer follow-up (Part 3) — cleared the pre-existing F-038 CI red via committed-evidence re-point, hardened F-039 (confidence≠validity sentence + repo-wide dated census).
Decision/Output: (1) F-038 CI red ROOT-CAUSED + FIXED: test_epistemic_invariants `_resolve_evidence` only matches `src|docs|results|...`-rooted paths; F-038 cited code package-relative (`config_layer/...`, `core/...`) so only the gitignored `results/research/_rr_probe2` token matched → 0 resolved → fail. Re-pointed Evidence to committed source `src/config_layer/rr/rr_fusion.py:35` (`_passthrough`) + `src/core/engine_runner.py:694` (final_score adoption/discard); softened the gitignored probe mention to non-load-bearing; appended a dated audit line to F-038 Note (conclusion UNCHANGED, history preserved §6.2 rule 4). (2) F-039 Note: added the reviewer's "L3 increases confidence but is not required for the validity of prior research conclusions, because the L1/L2 invariants remained active on all historical execution paths" (confidence≠validity). (3) F-039 Evidence upgraded to REPO-WIDE census, framed as a DATED OBSERVATION (2026-06-26, re-checked by Revalidate-by, explicitly NOT a future-code guarantee): only two validate_dataset sites (backtest_v2.py:2586/2769); the entire scripts/ fleet (training auto-tuners, all scripts/research/* drivers incl. those that produced F-019…F-035, all scripts/analysis/*) + tests/ also stream with the inline backstop only — closes the "hidden consumers" unknown. CLAUDE.md §6.2 F-039 index row re-worded to observation framing. GATES: test_epistemic_invariants + test_current_findings = 27 passed / 1 skipped / 0 FAILED (the F-038 red is gone).
Belief Update / ROI / Goal:
  Goal: every ledger finding independently resolvable+reproducible (epistemic invariant), without scope-creeping into re-opening architecture.
  Belief: the CI gate was behaving exactly as intended — it caught a finding whose citation violated the resolver contract (package-relative, gitignored), NOT a wrong conclusion. ARCH findings' reproducible evidence is committed CODE, not experimental outputs.
  Knowledge ROI: high — restored a green epistemic gate the honest way (committed evidence), crystallized confidence≠validity so "L3 only in backtest_v2" can't be misread as "findings suspect", and made the census falsifiable (dated, not eternal).
  Action: Part 3 complete; stayed strictly on the "fix evidence / clarify scope / close uncertainty" side of the line — did NOT commit probe artifacts, re-open the rr_fusion defect, or add runtime requirements.
Open Questions: (a) timeline.md still stale 06-14…06-26 (separate backfill pass, deferred); (b) whether research paths should eventually adopt L3 — separate, evidence-gated, NOT implied by F-039; (c) future contributors could add new validate_dataset sites → Revalidate-by (2026-09-24) re-verifies the census.
Next Step: task complete unless user wants the timeline backfill.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: timeline.md backfill — closed the stale 2026-06-14…06-26 window (the gap flagged during the F-039 work).
Decision/Output: Added 10 dated rows to docs/timeline.md (newest-first), sourced from authoritative finding Validated: dates + dated MEMORY entries (NOT reconstructed from loose memory): 06-26 (ingestion-trace verification + stream() integrity doctrine + F-039 + F-038 evidence repair), 06-25 (F-037 CRT-only spine + F-038 rr=gaussian + mt5 v0.4.0), 06-24 (F-036 zone top_k inert), 06-19 (F-035 FX generalization + MT5 fetch layer), 06-18 (F-032/F-033/F-034 panel+carry nulls + perp fetch + flow-context M1 + E-001), 06-17 (F-030 regime-LEVEL + F-031 E-001E governance catch), 06-16 (Framework Registry), 06-15 (F-029 lookahead-benign + Config-First doctrine frozen), 06-14 (F-028 PNF-v1 REJECTED/FROZEN), 06-13 (F-027 HTF Program 3A — was missing). Added Eras item 5 (research-falsification sweep + config-first + integrity hardening, 06-06→06-26). VALIDATION: all new data rows well-formed 4-col; no dedicated timeline test exists (docs-only truth-maintenance per §6.2 rule 6).
Belief Update / ROI / Goal:
  Goal: keep the chronological index (timeline.md) synchronized so cold-start sessions can place every finding in time (§6.2 rule 6).
  Belief: the timeline had drifted 13 days behind the findings ledger (F-027…F-039 absent); it is now current through 2026-06-26.
  Knowledge ROI: medium — no new conclusions, but restores the "what changed when" traversal path the knowledge-map depends on; prevents future sessions re-deriving the 06-14…06-26 arc from scattered findings.
  Action: timeline current; the earlier deviation (skipped lone F-039 row to avoid a misleading gap) is now fully resolved by backfilling the whole window.
Open Questions: none — the 06-20…06-23 gap is real (no recorded session work those days), not missing rows.
Next Step: none; timeline backfill complete.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26 09:30 IST
Topic: F-038 Fix A SHIPPED (configurable rr_fusion full-vector) — necessary but INSUFFICIENT; model is OOD even fed real features
Decision/Output: Implemented Fix A per owner: new config knob engine_runner.rr_fusion.full_feature_vector (bool, default FALSE = byte-identical legacy). engine_runner.run() now branches — when true, builds the full canonical feature dict from input_data (+gaussian_score/p_win) and calls the EXISTING RRFusionLayer.score() (full-vector → build_feature_vector → predict), instead of the 3-feature score_dict() stub. Added CANONICAL_FEATURES import; key added to v2 config rr_fusion block + ENGINE_RUNNER_DEFAULTS (soft .get default false for cross-config resilience). Test tests/test_rr_fusion_full_vector.py (3 green, fake-engine: stub feeds ≤3 features, score feeds ≥10). VALIDATION gate-ON: (parity, default off) BNB 11 sha aab9783b + SOL 6 a6f795fb — byte-identical to F-038 baseline ✓. (impact, full=true) full mode IS active (flag True, score() called 11×, score_dict 0×) and feeds 28-33 nonzero features (vs 2-3), lifting model confidence ~60 ORDERS OF MAGNITUDE (1e-88…1e-66 → 2.3e-25…5.0e-5) — BUT max conf 5e-5 STILL ≪ confidence_bypass_threshold 0.3 → 100% bypass persists → ledgers byte-identical, rr still == gaussian. So the defect has TWO layers: (1) feature starvation [Fix A resolves the interface], (2) rr_fusion model is OUT-OF-DISTRIBUTION/mis-calibrated on live bars even fed real features [UNRESOLVED]. Updated F-038 (FIX A line) + CLAUDE.md index; test_current_findings + test_doc_citations + rr_fusion test 13 green. (Probe bug caught + fixed: a predict-patch with positional param names broke the keyword call → inference_error; corrected signature to capture real confidence.)
Belief Update / ROI / Goal: Goal: make the RR engine a real independent fusion signal. Belief UPDATED: feeding the full vector is NECESSARY but NOT SUFFICIENT — the rr_fusion NanoInference model can't clear its own confidence gate even with real features (max 5e-5 ≪ 0.3), so it's OOD/mis-calibrated, not merely starved. Knowledge ROI: high — the "obvious" fix (feed full vector) was implemented + measured and shown to be a partial fix; the binding issue is model calibration. Action: keep full_feature_vector default FALSE (changes nothing live until model fixed); the real options are (a) recalibrate/retrain the rr_fusion model, (b) reconsider the 0.3 bypass threshold (but 5e-5 is so low that admitting it = noise), or (c) disable rr_fusion to restore the base RREngine (Fix B). Reaffirms: implement + MEASURE, don't assume a plausible fix works.
Open Questions: (1) Why is the model OOD even with full features — trained on a different feature representation / different bars / scaling mismatch? (2) Is the base RREngine (Fix B) actually independent+useful? (still unverified). (3) live-path unverified. (4) gaussian near-constant ~0.883 still open.
Next Step: Owner decision: recalibrate/retrain rr_fusion model, or pivot to Fix B (disable rr_fusion). Fix A stays as the correct interface (default off). Nothing committed; untracked tests/test_rr_fusion_full_vector.py + results/research/_rr_fixA*.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Verified the user's five-era worldview-evolution "time replay" + persisted it (with an Era-6 forward-look) into PROJECT_HISTORY.md; recorded an E-001 self-correction.
Decision/Output: Appended two append-only sections to docs/PROJECT_HISTORY.md (which previously stopped at 2026-06-10): "## Worldview Evolution — Five Eras (Apr–Jun 2026)" (belief→built→reality→worldview-shift per era, the F-019…F-039 falsification arc, cited by F-id only — no path:line) + "## The Next Question — Era 6 (open hypotheses, not promises)" (entry-info bottleneck F-025, execution-model constraint F-030, intelligence-not-scarce F-001, reopen-only-via-new-ontology; candidate untested threads = event-driven/transition ontology, execution-microstructure, perp OI Program 7, metals calendar, the F-038 rr_fusion defect; honest null = instruments may be efficient at M15 → the compounding asset is the falsification machine itself). Added 2 TOC entries. VERIFICATION baked into the narrative: F-002 stated as F-002→F-021→F-017 (selection collapsed to session, session non-improvable OOS), F-021 exact reject counts (ZONE 0/110, SCORE 2/110 noise p=0.327, SESSION 110/112≡ALL). GATES: test_doc_citations + test_topic_docs + test_current_findings = 13 passed; grep guard confirms F-id/doc-name citations only (no path:line). Authority caveat (§6.5) explicit: Era 6 = research-framed open questions, no promised edge.
Belief Update / ROI / Goal:
  Goal: preserve the project's *intellectual* evolution (the why behind the findings) so cold-start sessions inherit the worldview, not just the conclusions (§6.1 zero intelligence loss).
  Belief: the repository's deepest change is its QUESTION — from "build a trading system" to "build a machine that discovers whether its own beliefs are false." The five-era arc is accurate to the ledger.
  Knowledge ROI: high — captures meaning (the belief→falsification trajectory), which §6.1 says is the non-recoverable asset; future sessions can place any finding in its worldview context.
  Action: narrative persisted; Era 6 left as falsifiable open questions, not a roadmap.
E-001 SELF-CORRECTION (mandatory phrase): "Caught me overclaiming; I owe you a correction." I flagged the user's "F-021 Score: 2 rejects" as drifted; verification (current-findings.md:305) shows SCORE = 2/110 rejects EXACTLY — the user was precise, I was wrong. The persisted narrative carries the correct figure; the false flag lived only in chat and is retracted here (no recorded artifact carried it).
Open Questions: none for this task; Era-6 threads are pre-registration candidates, not commitments.
Next Step: task complete; PROJECT_HISTORY.md now current through 2026-06-26.
---

---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: v0.5.0 "Commission Semantics Verified" COMPLETE — IC Markets Raw, Case C, zero kernel change; project AT REST
Decision/Output: Executed the Level-3 commission sprint end-to-end on IC Markets Raw (Raw Trading Ltd/ICMarketsSC-Demo/52935582, hedging mm2). Gate 0 PASS (commission_charged=True, Σ −0.23 across 5 deals). Form = Case C (commission split entry+exit, FOLDED onto volume>0 trade deals; NOT a separate Case-B zero-volume deal). Kernel net_pnl=Σ(profit+swap+commission) summed it correctly: episodes net_pnl −0.08/−0.15 = the per-deal commissions; verify PASS (net_pnl_diff ≈2.8e-17, volume_diff 0.0, manifests OK), 2 pos→2 eps. 74 green. ZERO kernel change. Committed 59773f7 (MIGRATIONS + BROKER_VALIDATION) + memory. Plan files (working + tracked docs/implementation_plan) marked COMPLETE/AT-REST with earned scope + three reopen conditions.
Belief Update / ROI / Goal: Goal: close the strongest remaining broker-semantics assumption (commission==deal.commission) with real evidence. Belief: CONFIRMED on the first commission-charging AND first non-MetaQuotes broker — the kernel handles commission (Case C, split entry+exit) with zero change; reconstruction now validated across TWO broker families {MetaQuotes hedging/netting/INOUT/folded-swap + IC Markets commission}. Knowledge ROI: high — a major-yet-bounded result; the abstraction boundary held against a new broker family with no mutation. Earned scope is "validated across observed MT5 broker families and semantic forms", NOT "broker-independent". Action: kernel frozen; project AT REST.
Open Questions: Does any broker emit a SEPARATE zero-volume commission deal (Case B)? Exchange-margin mm1 semantics? Non-FX MT5 products?
Next Step: None operational. Reopen only on: (1) a Case-B separate-commission broker, (2) an mm1 exchange account, or (3) a real verify FAIL. Until then, preserve scope as earned.
---
