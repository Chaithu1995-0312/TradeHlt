<!-- ============================================================= -->
<!-- ARCHITECTURE MIGRATION DOCTRINE — keep this block at the top.  -->
<!-- Canonical companion docs live in docs/architecture/.          -->
<!-- ============================================================= -->

# ARCHITECTURE MIGRATION DOCTRINE

> **This file is the permanent record of every session decision since April 2026.**
> Every entry below carries a date and a decision summary in timestamped order, so you can scroll back through the full history without replaying old conversations.
> Governed by [`CLAUDE.md`](CLAUDE.md) §6 (Persistent Logging Mandate).
> Cross-reference: [`CLAUDE.md`](CLAUDE.md) §2 for the companion docs map.
---

**North star — LLM context economy.** We migrate toward an event-driven LLM-event-
microservices architecture so a future LLM loads only the *one* service it needs
(ins → flow → outs) instead of the whole codebase. Microservice boundaries = context
separation. Telemetry is curated into per-episode "LLM logs" so context does not grow
unbounded. Every application flow is documented as a loadable context unit.

**Priority order (ranks above refactor speed):**
replay correctness > explainability > telemetry continuity > advisory-AI >
(structure validity ≠ execution validity).

**The five governance questions — apply as a pre-merge checklist to every change:**
1. Does replay remain deterministic?
2. Does telemetry remain comparable across runs?
3. Can this state be audited later?
4. Can an LLM reason about this event?
5. Is execution authority still isolated?

**Standing rules:**
- **Consolidate on `src/events/event_fabric.py` — never fork the event system.**
- LLMs are advisory governance, **never** execution authority.
- No lookahead in replay; deterministic seeds mandatory; comparison ignores
  `event_id`/`generation`/wall-clock `timestamp`.
- New telemetry is additive; no field removed without a documented superseding field.

**Migration sequencing index** (detail in `docs/implementation_plan/`):
- **M0** — Map & doctrine: `docs/architecture/{CODEBASE_STATE_MAP,EVENT_TAXONOMY,
  SERVICE_BOUNDARY_MAP,REPLAY_GOVERNANCE,LLM_GOVERNANCE_LAYER}.md` + `services/`.
- **M1** — Telemetry normalization (envelope trade writers) + per-episode LLM log.
- **M2** — Event extraction (CRTState transitions + silent EventTypes).
- **M3** — Orchestration de-coupling (kill `live_engine_hook` singletons).
- **M4** — Dependency inversion (governance/analytics stop importing `runtime.backtest_v2`).
- **M5** — LLM-layer hardening (`GOVERNANCE_MODE`, decision-path assertions, advisory event contract).

<!-- ============================================================= -->
<!-- SESSION LOG (newest first). Append new entries below the       -->
<!-- doctrine block, above prior entries.                           -->
<!-- ============================================================= -->

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Migration - Batch A (4 live-spine modules) + Batch B (behavior_census evidence engine). Practice->Evidence; doctrine NOT yet frozen into CLAUDE.md.
Decision/Output: Migrated 4 live-spine modules from hardcoded BEHAVIORAL constants to config, FAIL-FAST (owner directive "no defaults, throw error, fail fast") - NOT R2 .get(default) soft pattern. A1 dynamic_threshold.py/decision_engine.py: _THRESHOLD_PERCENTILE/MIN/MAX -> decision_engine.threshold_* (resolved a doc/code split-brain: header CLAIMED config-driven, code was hardcoded; DynamicThreshold params now required). A2 regime_governor.py -> new regime_governor section + from_prod_config (INERT: ultron_gate_enabled=false). A3 convergence_controller.py -> new convergence_controller section + from_prod_config (INERT: fusion_use_evaluate=false). A4 acceptance_controller.py -> new acceptance_controller section + from_prod_config. All hash-neutral (params untouched). Built scripts/analysis/behavior_census.py (AST classifier STRUCTURAL/BEHAVIORAL/GOAL_SEEKING + config-wired detection; 67 behavioral/45 future opportunities) + tests/test_behavior_census.py regression floor. Staging doctrine docs/research-readiness/config-first-doctrine.md (NOT CLAUDE.md). PARITY BYTE-IDENTICAL on BNBUSDT f61b44ea...e4e9 + SOLUSDT d2721c2c...b591d (baseline==post-A1==combined). 118 module tests + 6 census/reachability green. 1 unrelated red (test_engine_runner_ml_impl, ML deps absent).
Belief Update / ROI / Goal: Goal: make behavior config-mutable so goal-seeking = generating configs, not editing engines. Belief: config-first is provable WITHOUT behavior drift - fail-fast + JSON-value==prior-literal gives byte-identical ledgers; A2/A3 confirmed currently inert. Knowledge ROI: high - established a repeatable from_prod_config strict-boundary pattern + an evidence engine (behavior_census) turning the doctrine into a test floor. Action: hold doctrine in STAGING until >=4 proven migrations + green census (now met); Batch C (CRTConfig ~30 params knobs, needs rehash) deferred.
Open Questions: confirm full determinism+oracle suite green (running at log time, expected green). When to freeze sec.2-4 into CLAUDE.md (criterion now met - owner call).
Next Step: confirm determinism/oracle; offer owner (a) freeze doctrine into CLAUDE.md, (b) Batch C CRTConfig split-brain, or (c) next census batch (crt_gaussian_scorer hard-filters).
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 2 - FREEZE doctrine into CLAUDE.md sec.6.5 + upgrade behavior_census with a maturity model. Owner directive: freeze now (before Batch C), GOAL_SEEKING orthogonal, strong fail-fast rule.
Decision/Output: Froze the Config-First doctrine into CLAUDE.md new sec.6.5 (doctrine cluster, between 6.4 and 7): principle (Frozen Engine + Mutable Behavior + Governed Evolution); META-RULE "Evidence outranks doctrine" placed immediately after principle (freeze only after implementation->parity->census->determinism, never reverse); 3 classes STRUCTURAL/BEHAVIORAL/GOAL-SEEKING; maturity ladder HARD_CODED->CONFIG_WIRED->CONFIG_DRIVEN with GOAL_SEEKING ORTHOGONAL (CONFIG_DRIVEN->GOAL_SEEKING, not every module a tuner); hard rule NO silent defaults (strict _require/from_prod_config, missing key=error, soft .get deprecated, parity=JSON==literal + strict read + byte-identical); 6 questions; enforcement contract behavior_census.py <-> test_behavior_census.py; Batch C note. Flipped staging doc header STAGING->FROZEN(2026-06-15). Upgraded behavior_census.py (additive, read-only): per-module maturity + maturity_summary + orthogonal is_goal_seeking marker + maturity table in MD; always-include tracked migrated modules; check_migrated now pins maturity. Verified: maturity CONFIG_DRIVEN 10 / CONFIG_WIRED 6 / HARD_CODED 6; dynamic_threshold=CONFIG_DRIVEN, regime/convergence/acceptance=CONFIG_WIRED (owner table exact). Tests: 7 census/reachability + 18 doc/findings-enforcement green. No code-path change (census read-only, CLAUDE.md prose) -> determinism/oracle untouched, no re-fingerprint. No F-id (engineering doctrine, not a system finding).
Belief Update / ROI / Goal: Goal: make the proven config-first philosophy CONSTRAIN future work instead of waiting on Batch C. Belief: doctrine has crossed from aspiration to experimentally-verified repository truth (4 byte-identical migrations + census + determinism all green) - so freezing is harvesting the lesson, not premature. Knowledge ROI: high - sec.6.5 + the maturity ladder turn "magic-number count" into a maturity model that future migrations (incl. Batch C) inherit; "Evidence outranks doctrine" is the transferable meta-rule. Action: doctrine frozen; Batch C (CRTConfig ~30 params knobs, needs rehash) becomes the FIRST consumer of the doctrine, not its justification.
Open Questions: none blocking. Batch C sequencing (when owner wants it).
Next Step: Batch C - CRTConfig params split-brain repair under the now-frozen sec.6.5 (rehash via scripts/maintenance/_compute_hash.py); or next census HARD_CODED batch (crt_gaussian_scorer hard-filters). Owner's call.
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 3 - evidence-guided prioritization: extra doctrine (precedence hierarchy + authority ladder), SHARPEN behavior_census (external-injection blind spot), migrate PROMOTION_MARGIN, re-rank. Owner sequence: fix the biased instrument BEFORE migrating.
Decision/Output: WI-0 froze into CLAUDE.md sec.6.5 + staging doc: precedence hierarchy Evidence>Doctrine>Preference>Convenience>Elegance; "Evidence quality outranks migration quantity"; "Never optimize using a biased measurement instrument"; AUTHORITY LADDER (anti-self-deception, generalizes beyond config, ties sec.6.1+G001): permanent invariant "evidence has no authority; only demonstrated G001 improvement grants authority"; 4 distinct states Information->Economic usefulness->Authority earned->Architecture justified; corollaries Information!=value, value!=authority, authority!=architecture; REGIME_EXPLOITABLE=information not authority, only CONSUMER_CANDIDATE w/ measured dG001 earns authority; CONFIG_DRIVEN grants tunability not authority. WI-1 sharpened behavior_census: corpus config-key-read scan + curated _CONFIG_SCHEMA_CLASSES{CRTConfig,FusionConfig} + *Result/Record field defaults=STRUCTURAL placeholders; per-const externally_wired/hardcoded split. EFFECT: future config opportunities 45->7, HARD_CODED 6->5, fusion_engine HARD_CODED->CONFIG_WIRED. MAJOR CORRECTION: crt_engine_v2 "27" = 24 externally-wired CRTConfig fields + 3 genuine state defaults => "Batch C" is config-COMPLETENESS (populate active params/crt_engine + rehash) NOT engine surgery. WI-2 migrated model_registry.PROMOTION_MARGIN=0.02 -> governance.promotion_margin via ModelRegistry.from_prod_config() fail-fast (eager singleton); value unchanged 0.02; 91 model_registry/promotion tests green. WI-3 recorded sharpened ranking in staging doc (A5 row + Phase-3 section). Verified: 26 census/reachability/doc-enforcement green; determinism spot-check BNBUSDT f61b44ea...e4e9 BYTE-IDENTICAL (governance/census edits don't touch spine). New test asserts fusion_engine not over-reported.
Belief Update / ROI / Goal: Goal: use the census AS INTENDED (evidence->prioritization->migration) instead of picking Batch C by habit. Belief: the prioritization instrument was BIASED (over-reported HARD_CODED via external-injection blind spot) - fixing it first was higher-leverage than any migration; the genuine backlog is tiny (7 opps) and Batch C is much smaller/different than feared (config-completeness, not 30-knob engine surgery). Knowledge ROI: high - sharpened census now gives trustworthy rankings for all future migrations; PROMOTION_MARGIN harvested as the clean genuine win; authority ladder is the transferable anti-self-deception doctrine. Action: trustworthy backlog established; remaining genuine HARD_CODED are advisory/dormant/3-state-defaults; Batch C = config-completeness task (owner's call, needs rehash).
Open Questions: Batch C (CRTConfig active-config completeness + rehash) timing - owner's call. The 3 crt_engine_v2 state defaults (decay_factor/risk_pct/soft_conf_candles) - migrate or leave (low value).
Next Step: owner decision: Batch C config-completeness (populate params/crt_engine + rehash) OR sweep remaining tiny HARD_CODED OR stop (backlog now trustworthy + small).
---

---
SESSION LOG ENTRY
Date: 2026-06-15
Topic: Config-First Phase 4 (Batch C) - CRT config-completeness, REFRAMED by Phase-3 instrument repair: NOT engine surgery, NOT rehash. Hash-neutral crt_engine population + state-default classification.
Decision/Output: Investigation: load_prod_config builds CRTConfig={**crt_engine,**params}, hash covers params ONLY -> crt_engine is the unhashed defaults layer. Exact gap = 12 CRTConfig fields absent from crt_engine+params (of 48), each resolving to dataclass default. WI-C1: populated all 12 into crt_engine with value==dataclass default (breakout_disp_threshold 1.5, exit_model 'intrabar_touch' [load-bearing governing exit, previously IMPLICIT], score_threshold 0.45, use_bitnet false, extension_reset_fib 1.618, pending_displacement_ttl_candles 4, max_expansion_age_candles 495/hours 124, expansion_age_warn_candles 342, shadow_advisory_only false, shadow_age_norm_candles 0, shadow_age_penalty_lambda 0.0). HASH-NEUTRAL, NO rehash (owner: crt_engine=defaults layer, params=tuned/hashed). allowed_sessions EXCLUDED (engine_runner-owned via resolve_allowed_sessions; would create new split-brain). PARITY BYTE-IDENTICAL BNB f61b44ea...e4e9 + SOL d2721c2c...b591d. WI-C2: 3 state defaults (RiskScore.decay_factor/Trade.risk_pct/EngineState.soft_conf_candles) classified STRUCTURAL placeholders (real knobs score_decay_lambda/sizing_bands/soft_conf_max_candles already config-driven) via census _RESULT_SCHEMA_RE+State + _RUNTIME_STATE_CLASSES -> crt_engine_v2 genuine HARD_CODED 3->0 -> CONFIG_WIRED. WI-C3: added §6.5 + staging-doc corollary "correcting the instrument often creates more value than optimizing the system". WI-C4: census re-run HARD_CODED 5->3, opportunities 7->3 (only signal_audit advisory + signal_belief_tracker sidecar + HMF dormant remain). Verified: config loads+hash OK; 27 census/reachability/doc-enforcement green; new crt_engine_v2 + fusion_engine census assertions green. determinism+oracle suite running (corroboration; fingerprint already byte-identical).
Belief Update / ROI / Goal: Goal: close the F-018 CRT split-brain (config = explicit source of truth). Belief: Batch C was MUCH smaller than the original "30-knob engine surgery + rehash" fear - the instrument repair revealed it as a 12-field hash-neutral config-completeness pass; the program has reached its meaningful END (every live behavior-affecting knob now config-driven; remaining 3 are advisory/sidecar/dormant). Knowledge ROI: high - "Batch C needs rehash" belief RETIRED (evidence outranks expectations); the governing exit_model is now explicit in config (was implicit). Action: config-first migration program COMPLETE for live knobs; remaining HARD_CODED are intentionally skipped (advisory/sidecar/dormant).
Open Questions: none blocking. Optional micro-items: signal_audit warn thresholds (advisory) if ever wanted; otherwise STOP - backlog is intentionally empty of high-value items.
Next Step: program effectively done. Optional: commit the work; or migrate the 3 advisory/sidecar knobs only if their modules become live (don't - F-012 theater). Goal-seeking (generating candidate configs over the now-complete crt_engine) is the natural next CAPABILITY, separate from migration.

---
📝 SESSION LOG ENTRY
Date: 2026-07-01
Topic: Deploy F-038 fix — commit ACTIVE_VERSION flip + Phase B economic truth audit on patch
Decision/Output: Staged and committed exactly 4 scoped files on `patch` (working tree has ~80 unrelated pre-existing modifications + .env, deliberately NOT included): configs/production/ACTIVE_VERSION (flip v2_multi_2026_04 → deploys F-038 rr_fusion.enabled=false), docs/governance/finding_dependency_audit.md (new; Phase A 12/12 + Phase B), docs/current-findings.md (F-019/021/023 revalidation), assistant_project.md (session logs). Result artifacts results/research/*_2026_06_27/ are gitignored (not committed). Pre-commit hook ran suite (71 passed, 1 skipped) then required a same-day (2026-07-01) SESSION LOG entry for the governed configs/production/** change — this entry satisfies that linkage.
Belief Update / ROI / Goal: Goal: ship the F-038 deploy so the committed runtime truth matches the working-tree fix. Belief: the ACTIVE_VERSION flip alone deploys F-038 (resolver loads v2_multi_2026_04.json enabled:false); the Phase-B replays already proved the deploy does NOT change research-spine economics (byte-identical, F-037). Knowledge ROI: mechanical (deploy). Action: F-038 committed; entry-info null unchanged; binding constraint still execution model.
Open Questions: The ~80 other uncommitted working-tree files + .env remain unstaged (out of scope, owner's call). Machine clock (2026-06-27 in run logs) is skewed vs harness date (2026-07-01).
Next Step: none required; F-038 deployed on patch. Optionally push, or continue lower-priority R2 replays (all low reversal risk).
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Provider-aware MT5 data layer — autoderive calendar + data/mt5 corpus + M5 depth-probe + crypto discovery
Decision/Output: After the full fetch revealed broker-calendar mismatch + M5-retention limit (prior entry), implemented the user's 4 chosen designs (auto-derive calendar / probe+max-recent M5 / auto-discover crypto / separate data/mt5 root). BUILT: (1) src/data_ingestion/session_autoderive.py — derive_weekly_mask learns each provider's tradable (weekday,minute) slots from the data's modal weekly presence (presence_min=0.5); is_tradable_by_mask = mask ∧ ¬holiday. Hooked into _is_tradable via sc['_weekly_mask']; validate_dataset builds the mask when cfg tradability_mode=autoderive. EMPIRICALLY VALIDATED on real IC-Markets data: EURUSD M15 mask learned exactly 480 slots = 96/day×5 weekdays (NO Sunday, NO 21:00 break) → the Sunday/break false-rejects vanished, residual = genuine holidays only. (2) config strict_fetch gained tradability_mode=autoderive + autoderive_presence_min; session_calendar.holidays populated with 24 reviewed dates (FX Christmas/NewYear + XAUUSD US-market calendar from the data) — micro-gaps (2026-03-25/04-01, 2025-06-02/07-30) DELIBERATELY excluded so they keep hard-stopping. (3) fetcher: MT5CandleFetcher.discover_symbols (path-based 'Crypto\' → 17 CFDs: BTCUSD/ETHUSD/SOLUSD/... precise, name-substrings over-matched equities) + fetch_recent = copy_rates_range DEPTH-PROBE (from_pos reads empty cache; range triggers download) → EURUSD M5 served deepest ~270 days/55239 bars. (4) fetch_and_verify_mt5: default --out data/mt5 (provider-tagged, separate from Binance), --discover-crypto, M5 range-empty→max-recent fallback; _missing_tradable diagnostic ALIGNED to the autoderive mask (was showing phantom Sunday slots). FIX (E-001): holidays were applied universally incl. crypto → broke test_excess_missing_pct_raises (_BASE=2025-01-01=a listed holiday); corrected — config-mode CRYPTO is 24x7 and ignores TradFi holidays (holidays apply to WEEKDAY markets + autoderive only). Tests: +12 (test_session_autoderive 7, strict_fetch crypto-holiday guard) ; 44 data_ingestion + 225 integrity/research/findings green. CORRECTION carried from prior turn: data CSVs are GITIGNORED (not git-tracked); restored FX M15 from quarantine.
Belief Update / ROI / Goal: Goal: a strict, trainable, provider-differentiated multi-TF corpus. Belief: tradability should be LEARNED per provider (modal weekly mask) not hardcoded — this self-calibrates to any broker (no-Sunday / shifted-open / CFD crypto) and reduces holiday curation to a tiny reviewed list. Confirmed: autoderive turned ~all FX/metals false-rejects into a clean separation of {real holidays → whitelist} vs {real micro-gaps → keep flagged}. Residual truth: the broker's data has genuine 4-21 bar micro-holes (esp. 2026-04-01) that correctly hard-stop under zero-tolerance; M5 retention ≈270 days (not 2yr). Knowledge ROI: high — the gate converted silent data assumptions into explicit, surfaced facts (broker calendar, M5 depth, exact holes), exactly the inconsistency the user wanted blocked. Action: deliver the bulk-fetch command; the 2026-04-01 micro-gap + sub-2yr M5 are standing user decisions (re-fetch later / accept-list / shorter window), NOT code work.
Open Questions: Resolve 2026-04-01 (+ other) micro-gaps — re-fetch (broker may fill) vs explicit accept-list vs accept shorter clean window? Should holidays be per-market (FX vs metals) rather than a global union (currently safe via harmless over-marking)? Full bulk fetch (6 FX/metals + 17 crypto × 4 TF) deferred — heavy + needs interactive micro-gap review.
Next Step: User runs `python scripts/data/fetch_and_verify_mt5.py --discover-crypto` for the full corpus when ready; review quarantined micro-gaps; decide micro-gap policy. Commit when asked.
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Model-Intent Forensics → Evidence-Driven Gated Feature Expansion — plan approved + Phase 1 shipped
Decision/Output: Approved (3 review rounds) the plan at C:\Users\Hi\.claude\plans\d-tradelatest-reports-ohlcv-lineage-for-glistening-wilkinson.md. Reframed the user's "make 38 features 100% + train" into THREE separate questions — A=intended ownership, B=marginal model value, C=ensemble-uniqueness — + a 4th objective (100% ensemble robustness) + a Net-Contribution admission rule (ADMIT iff A∧B∧C∧M4∧ΔG001>0 else KEEP SPECIALIZED; Net = ΔG001 − Correlation Penalty − Complexity Penalty). Measure-first, gated retrain, no deletions, advisory-first (§6.5). Phase 1 SHIPPED: (1) created docs/topics/model-intent-and-feature-ownership.md — canonical authority: per-model intent (Question A verified w/ file:line) + full 38×model ownership matrix (A done; B/C cells = CAND·B evidence-pending Phase 3/4). (2) RESOLVED the registry "split-brain": runtime scores through models/zone_registry.json (config engine_runner.zone_registry_path, zone_mode=hard) = 8 zones, 6/8 negative mean_RR, ~98% SL-hit labels; zone_gate_registry.json is a VERSION MANIFEST whose active:true points to a DIFFERENT file (sha aade29c4≠e73e0893) → TruthConflict surfaced not auto-reconciled. (3) E-001 self-correction: my first probe's "manifest empty (0 zones)" was a parse artifact (keyed by version, not a zones list) — corrected in F-041 Reversal. Registered F-041 (GOV/Certain/OPEN) in current-findings.md + CLAUDE.md Truths Index; added topics/readme row. Tests: test_topic_docs + test_current_findings = 11 passed.
Belief Update / ROI / Goal: Goal: decide whether feature/label changes move G001 without destroying ensemble diversity. Belief: the binding ZoneGate problem is LABEL/EXIT quality (98% SL training labels), NOT feature count — it already consumes all 38; "use all 38 everywhere" would erase deliberate CRT/RR specialization and risk correlated errors (Question C). Whether labels are the dominant defect is the explicit Phase-5 gate, NOT asserted yet. Knowledge ROI: high — reframed a known-null entry-info intervention (F-019…F-040) toward the untested label-quality axis; the [TO VERIFY] discipline already caught one of my own overclaims. Action: proceed to Phase 3 (static zone-weight read + edge_attribution_study.py) feeding the B/C cells; keep no production/model writes.
Open Questions: Does the manifest active-flag divergence (F-041) need reconciling (user decision)? Does the stored ~98% SL meta reflect honest intrabar_fixed outcomes vs a labeling artifact (Phase 5)?
Next Step: Phase 3 — scripts/research/zone_weight_introspection.py (per-feature weights + zero_indices) + run scripts/research/edge_attribution_study.py → docs/analysis/feature-importance-2026-06-27.md.
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Documentation Drift Protocol (missing governance invariant) + F-038 live drift reconciliation
Decision/Output: User proposed a "Documentation Drift Protocol" + "No Silent Truth Changes" rule using F-038 as motivating example. PROBE found two things: (1) ~80% already lived in CLAUDE.md §6.2 (DOC_DRIFT/CODE_DRIFT/AMBIGUOUS classify=Step1, TruthConflict surface+ask=Step2/3, Sync mandates=Step4, CORRECTED-not-delete+audit=Step5) — so EXTEND, not duplicate (§6.2 rules 1/5). (2) The F-038 example was a LIVE split-brain: committed HEAD `ACTIVE_VERSION`=`v2_multi_2026_04 - deepdeektry` → resolver (production_config.py:60→:116) loads `… - deepdeektry.json:50` enabled:true (fix NOT deployed); working-tree (uncommitted) `ACTIVE_VERSION`=`v2_multi_2026_04` → `…_04.json:34` enabled:false (deployed once committed). The finding's "(the file ACTIVE_VERSION points to)" was false at HEAD; the audit row also mis-cited deepdeektry's :50 as `v2_multi_2026_04.json`. SHIPPED (3 user decisions locked via AskUserQuestion: thin-rule+long-form placement / gate only ambiguous+downgrade / reconcile F-038 now): (A) docs/governance/DOCUMENTATION_DRIFT_PROTOCOL.md — invariant ("every code/config/runtime-truth change triggers a documentation-truth decision"), 5-step process cross-linking §6.2, audit-trail format, completion criterion, F-038 worked example, no-new-authority. (B) CLAUDE.md §6.2 thin-rule subsection + §2 companion-docs table row. (C) tests/governance/test_documentation_drift_protocol.py (6 presence-checks, green). (D) Part B reconciliation: current-findings.md F-038 CORRECTED note (deploy-state, conclusion unchanged), finding_dependency_audit.md FLAG→RESOLVED across detail+2 summary tables + refreshed F-016 row + new Audit Log row, memory (project_zone_gate_topk_config correction + new project_documentation_drift_protocol + MEMORY.md index). Per the gate I just wrote, finding/audit DOC fixes were auto-fixable (sharpen a deploy claim, no conclusion downgrade); the two-config decision is GATED → surfaced to user, NOT auto-resolved. Tests: drift floor 6 + current_findings + doc_citations + topic_docs + epistemic = 38 passed/1 skipped.
Belief Update / ROI / Goal: Goal: zero silent truth divergence (the §6.2/§6.1 north star). Belief: §6.2 had the classification machinery but lacked a REQUIRED COMPLETION STEP — "a turn that finds drift is incomplete until the doc decision is recorded" — which is exactly why F-038's "fix shipped" header outran deployed reality. The new invariant + completion criterion closes that gap; the calibrated gate (auto-fix clear DOC_DRIFT, approve only ambiguous/downgrade/active-config) keeps it low-friction so it actually gets followed. Confirmed the protocol's own premise was a real, in-flight drift — best possible worked example. Knowledge ROI: high — turned a process recommendation into doctrine + a test floor + a corrected live finding in one turn. Action: get the user's call on the residual deepdeektry TruthConflict + committing the ACTIVE_VERSION flip; nothing committed yet.
Open Questions: Archive vs keep `configs/production/v2_multi_2026_04 - deepdeektry.json` (enabled:true/gaussian_impl:ml) vs the active `…_04.json` (false/heuristic)? Commit the working-tree `ACTIVE_VERSION=v2_multi_2026_04` flip to make the F-038 deploy real at HEAD? finding_dependency_audit.md is still UNTRACKED — commit it?
Next Step: User decides the two-config TruthConflict + whether to commit ACTIVE_VERSION/audit doc; then optionally /code-review the doctrine additions before commit.
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Finding Dependency Audit — F-038 reasoning self-correction + Phase B economic replays kicked off
Decision/Output: "Continue" on docs/governance/finding_dependency_audit.md. (1) Drafted plan to "correct F-038 as a misread (DOC_DRIFT)" — then CAUGHT MYSELF: I had read only the working-tree config. Verified committed state: `git show HEAD:configs/production/ACTIVE_VERSION` = `v2_multi_2026_04 - deepdeektry` (resolver production_config.py:116 → that file, rr_fusion.enabled:true at line 50, confirmed in archived copy). So the audit was CORRECT at its HEAD snapshot — fix genuinely not deployed in committed state. My "misread" framing was wrong. (2) Discovered Part 1 was already resolved externally (concurrent edit) more accurately than planned: F-038 FLAG→RESOLVED(pending commit) + residual deepdeektry TruthConflict resolved (git mv to ..._archived_20260627_160227.json; research_config_spine.json repointed; deepdeektry found non-loadable on patch — missing governance.promotion_margin). Left all of it untouched (no clobber, §6.2). (3) Launched Phase B (all 3 replays) in background: B2/F-019 qualify_majors (→ qualification_2026_06_27), B1/F-023 trade_anatomy (→ ..._2026_06_27), B3/F-021 phase_s (→ phase_s_2026_06_27). B2 early arm: expansion_breakout/mean_reversion all PF<1 E<0 → tracking 0 PROMOTE.
Belief Update / ROI / Goal: Goal: keep repository truth non-divergent (§6.2) + answer the E3 question (is post-stabilization a new economic universe?). Belief: the audit's F-038 FLAG was correct-at-snapshot, NOT a misread; I almost contaminated the record with a wrong correction by reading working-tree instead of committed HEAD. Knowledge ROI: high (process) — verify committed state when an audit cites a commit SHA; git status "M" on a pointer file = split-brain risk. Action: do not edit Part 1; complete Phase B and record results.
Open Questions: Will B1/B2/B3 confirm (E3 = stabilized E2) or break any finding (E3 = new universe)? Committing the ACTIVE_VERSION flip is still pending (user-gated).
Next Step: await the 3 background replays; verify each success criterion; update audit R2/Audit-Log rows + current-findings.md (Validated 2026-06-27) per Findings Mandate; if any breaks, run E-001 ritual before registering.
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Committed the session's work (candle-state F-040 + strict MT5 data layer)
Decision/Output: git commit 9eec682 on branch `patch` — 27 files, +4148. Scoped to THIS session only: candle_state/ research layer + compression_breakout + drivers + pre-reg + F-040 (current-findings + CLAUDE index); data layer (session_autoderive, extended dataset_integrity + mt5_candle_fetcher, config strict_fetch+holidays) + tests. Deliberately EXCLUDED: scripts/data/fetch_and_verify_mt5.py (scripts/data is gitignored by repo convention, like its sibling fetch_candles_mt5.py), data/mt5 corpus (gitignored), and the dozens of unrelated pre-existing src/ modifications from prior sessions (left uncommitted). One harmless rider: a pre-staged 0-diff archived-config rename. Not pushed.
Belief Update / ROI / Goal: none (mechanical commit).
Open Questions: Force-track scripts/data/fetch_and_verify_mt5.py despite the gitignore convention? Push `patch`?
Next Step: Await user — push and/or resolve the standing micro-gap / full-bulk-fetch decisions.
---
📝 SESSION LOG ENTRY
Date: 2026-06-27
Topic: Finding Dependency Audit — Phase B (Economic Truth Audit) executed; all three findings SURVIVE → Epoch-3 = stabilized Epoch-2
Decision/Output: Ran the 3 Phase-B economic replays on stabilized HEAD. (B2/F-019) qualify_majors → 0 PROMOTE; toy AND spine arms BYTE-IDENTICAL to the 2026-06-12 frozen baseline (verdicts {11 REJECT,4 INSUFFICIENT}); spine byte-identity corroborates F-037 (backtest gate-OFF/CRT-only → the F-038 rr_fusion disable never reaches research-spine entries). (B1/F-023) trade_anatomy under governing forward_walk(intrabar_fixed) labels → 4 KMeans clusters WR 0.3345–0.3428, mean_R −0.0001…+0.0228, n=139,942 (sha f8bdabbe). (B3/F-021) phase_s → SELECTION_IS_SESSION_ONLY (SESSION 112 p=0.0005, ZONE 0, SCORE 2 OOS−0.291 p=0.327). No finding broke → no E-001 reversal ritual. Updated docs: current-findings.md F-019/021/023 each +Update line, Revalidate-by→2026-09-25; finding_dependency_audit.md R2 rows ✅ SURVIVED + Audit Log Phase B row. Verify: pytest tests/test_current_findings.py tests/test_doc_citations.py → 10 passed. Artifacts in results/research/*_2026_06_27/.
Belief Update / ROI / Goal: Goal: answer the audit's central E3 question (is post-stabilization a new economic universe?). Belief: NO — stabilization (38-feat integration, fusion cleanup, F-038/config corrections) is pure cleanup; the entry-information null (F-019/021/023) is unchanged, byte-identical where reproducible. Knowledge ROI: high — closes the "did we accidentally create/destroy an edge while stabilizing?" question with a determinism proof, and the spine byte-identity independently re-confirms F-037 (gate-OFF). Action: stop suspecting stabilization moved the economics; the binding constraint remains the execution model / entry information, not codebase drift. Phase C (R3 ontology replays) stays deferred.
Open Questions: Committing the working-tree ACTIVE_VERSION flip (F-038 deploy) is still user-gated. Phase B baselines were re-run only on the 4 majors + BNB anatomy; lower-priority R2 findings (F-009/011/014/015/020/024/029/030/032/035) remain NOT DONE (low reversal risk per audit).
Next Step: optionally commit the ACTIVE_VERSION flip + the Phase-B doc updates; otherwise Phase B is complete and the audit's headline question is answered.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26 09:30 IST
Topic: F-038 Fix A SHIPPED (configurable rr_fusion full-vector) — necessary but INSUFFICIENT; model is OOD even fed real features
Decision/Output: Implemented Fix A per owner: new config knob engine_runner.rr_fusion.full_feature_vector (bool, default FALSE = byte-identical legacy). engine_runner.run() now branches — when true, builds the full canonical feature dict from input_data (+gaussian_score/p_win) and calls the EXISTING RRFusionLayer.score() (full-vector → build_feature_vector → predict), instead of the 3-feature score_dict() stub. Added CANONICAL_FEATURES import; key added to v2 config rr_fusion block + ENGINE_RUNNER_DEFAULTS (soft .get default false for cross-config resilience). Test tests/test_rr_fusion_full_vector.py (3 green, fake-engine: stub feeds ≤3 features, score feeds ≥10). VALIDATION gate-ON: (parity, default off) BNB 11 sha aab9783b + SOL 6 a6f795fb — byte-identical to F-038 baseline ✓. (impact, full=true) full mode IS active (flag True, score() called 11×, score_dict 0×) and feeds 28-33 nonzero features (vs 2-3), lifting model confidence ~60 ORDERS OF MAGNITUDE (1e-88…1e-66 → 2.3e-25…5.0e-5) — BUT max conf 5e-5 STILL ≪ confidence_bypass_threshold 0.3 → 100% bypass persists → ledgers byte-identical, rr still == gaussian. So the defect has TWO layers: (1) feature starvation [Fix A resolves the interface], (2) rr_fusion model is OUT-OF-DISTRIBUTION/mis-calibrated on live bars even fed real features [UNRESOLVED]. Updated F-038 (FIX A line) + CLAUDE.md index; test_current_findings + test_doc_citations + rr_fusion test 13 green. (Probe bug caught + fixed: a predict-patch with positional param names broke the keyword call → inference_error; corrected signature to capture real confidence.)
Belief Update / ROI / Goal: Goal: make the RR engine a real independent fusion signal. Belief UPDATED: feeding the full vector is NECESSARY but NOT SUFFICIENT — the rr_fusion NanoInference model can't clear its own confidence gate even with real features (max 5e-5 ≪ 0.3), so it's OOD/mis-calibrated, not merely starved. Knowledge ROI: high — the "obvious" fix (feed full vector) was implemented + measured and shown to be a partial fix; the binding issue is model calibration. Action: keep full_feature_vector default FALSE (changes nothing live until model fixed); the real options are (a) recalibrate/retrain the rr_fusion model, (b) reconsider the 0.3 bypass threshold (but 5e-5 is so low that admitting it = noise), or (c) disable rr_fusion to restore the base RREngine (Fix B). Reaffirms: implement + MEASURE, don't assume a plausible fix works.
Open Questions: (1) Why is the model OOD even with full features — trained on a different feature representation / different bars / scaling mismatch? (2) Is the base RREngine (Fix B) actually independent+useful? (still unverified). (3) live-path unverified. (4) gaussian near-constant ~0.883 still open.
Next Step: Owner decision: recalibrate/retrain rr_fusion model, or pivot to Fix B (disable rr_fusion). Fix A stays as the correct interface (default off). Nothing committed; untracked tests/test_rr_fusion_full_vector.py + results/research/_rr_fixA*.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: stream() "more than CSV parsing" doc + new finding F-039 (L3 dataset-integrity pre-flight is backtest_v2-only); conceded two review corrections on the no-lookahead explanation.
Decision/Output: (1) Conceded review: no-lookahead is the CONJUNCTION of L1 schema + L2 temporal + L3 dataset-intel + RT generator causal-ordering (not one gate); volume requirement flows schema->columns, not loader->volume. (2) Static call-site census (grep CandleLoader/validate_dataset across src/) resolved the open "does every caller run L3?" -> NO: validate_dataset is wired ONLY into backtest_v2 (_preflight_dataset:2769 + validate_universe:2586); all other stream() consumers (entire src/research/ qualification pipeline, sl_tp_comparator, portfolio_validation, exit_model_band, unified_replay_harness, config_validator) rely solely on the always-on inline L1/L2 backstop. (3) Registered finding F-039 (ARCH/VALIDATED/Certain) in docs/current-findings.md + CLAUDE.md §6.2 index (pair test-enforced), with E-001 scope guard: does NOT invalidate F-019..F-035 (inline backstop DID enforce schema+chronology) — flags a single-layer fragility. (4) Fixed DOC_DRIFT: dataset_integrity.py:24 docstring "every backtest entry point calls" -> real scope. (5) Added "Why stream() is more than CSV parsing" section to docs/topics/feature-schema.md (double-entry-bookkeeping analogy, 4-layer model, generator-as-causal-constraint, schema->columns) + L3-coverage table to signal-flow.md Step 1. GATES: test_current_findings + test_topic_docs + test_doc_citations PASS (13); test_epistemic_invariants 31 passed/1 skipped/1 FAILED — the fail is PRE-EXISTING F-038 (prior uncommitted session; evidence cites gitignored results/research/_rr_probe2), NOT F-039 (Certain, evidence resolved). Grep guard clean (no "every backtest entry point" in src/). DEVIATION: skipped the planned timeline.md F-039 row — the dated log is already stale at 2026-06-13 (F-029..F-038 absent), so a lone 06-26 row above a 13-day gap would mislead; backfill is out of scope.
Belief Update / ROI / Goal:
  Goal: zero silent truth divergence on the ingestion integrity model (§6.2) + compound the reviewer's sharpening.
  Belief: the no-lookahead guarantee is layered (L1/L2/L3+generator); on the research corpus the INLINE L1/L2 backstop is the SOLE integrity net (L3 = backtest_v2 only). The inline backstop is load-bearing, not redundant.
  Knowledge ROI: high — resolved a standing unknown with an exhaustive census, corrected a real docstring overclaim, and registered the architecture as F-039 so future sessions can't silently assume universal L3. Also surfaced a pre-existing CI red (F-038 evidence).
  Action: documented the asymmetry; do NOT add L3 to research paths without separate evidence-gating (Authority Ladder: information->doc/research authority only).
Open Questions: (a) F-038 evidence-resolvability is red (prior session) — re-point to a committed artifact or commit the probe output? (b) timeline.md is stale 06-14..06-25 — backfill F-029..F-039 as a separate pass? (c) MEMORY.md is over its size budget — F-039 left in the findings ledger (authoritative) rather than added to the over-budget index.
Next Step: await user steer on F-038 red + timeline backfill; otherwise task complete.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Reviewer follow-up (Part 3) — cleared the pre-existing F-038 CI red via committed-evidence re-point, hardened F-039 (confidence≠validity sentence + repo-wide dated census).
Decision/Output: (1) F-038 CI red ROOT-CAUSED + FIXED: test_epistemic_invariants `_resolve_evidence` only matches `src|docs|results|...`-rooted paths; F-038 cited code package-relative (`config_layer/...`, `core/...`) so only the gitignored `results/research/_rr_probe2` token matched → 0 resolved → fail. Re-pointed Evidence to committed source `src/config_layer/rr/rr_fusion.py:35` (`_passthrough`) + `src/core/engine_runner.py:694` (final_score adoption/discard); softened the gitignored probe mention to non-load-bearing; appended a dated audit line to F-038 Note (conclusion UNCHANGED, history preserved §6.2 rule 4). (2) F-039 Note: added the reviewer's "L3 increases confidence but is not required for the validity of prior research conclusions, because the L1/L2 invariants remained active on all historical execution paths" (confidence≠validity). (3) F-039 Evidence upgraded to REPO-WIDE census, framed as a DATED OBSERVATION (2026-06-26, re-checked by Revalidate-by, explicitly NOT a future-code guarantee): only two validate_dataset sites (backtest_v2.py:2586/2769); the entire scripts/ fleet (training auto-tuners, all scripts/research/* drivers incl. those that produced F-019…F-035, all scripts/analysis/*) + tests/ also stream with the inline backstop only — closes the "hidden consumers" unknown. CLAUDE.md §6.2 F-039 index row re-worded to observation framing. GATES: test_epistemic_invariants + test_current_findings = 27 passed / 1 skipped / 0 FAILED (the F-038 red is gone).
Belief Update / ROI / Goal:
  Goal: every ledger finding independently resolvable+reproducible (epistemic invariant), without scope-creeping into re-opening architecture.
  Belief: the CI gate was behaving exactly as intended — it caught a finding whose citation violated the resolver contract (package-relative, gitignored), NOT a wrong conclusion. ARCH findings' reproducible evidence is committed CODE, not experimental outputs.
  Knowledge ROI: high — restored a green epistemic gate the honest way (committed evidence), crystallized confidence≠validity so "L3 only in backtest_v2" can't be misread as "findings suspect", and made the census falsifiable (dated, not eternal).
  Action: Part 3 complete; stayed strictly on the "fix evidence / clarify scope / close uncertainty" side of the line — did NOT commit probe artifacts, re-open the rr_fusion defect, or add runtime requirements.
Open Questions: (a) timeline.md still stale 06-14…06-26 (separate backfill pass, deferred); (b) whether research paths should eventually adopt L3 — separate, evidence-gated, NOT implied by F-039; (c) future contributors could add new validate_dataset sites → Revalidate-by (2026-09-24) re-verifies the census.
Next Step: task complete unless user wants the timeline backfill.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: timeline.md backfill — closed the stale 2026-06-14…06-26 window (the gap flagged during the F-039 work).
Decision/Output: Added 10 dated rows to docs/timeline.md (newest-first), sourced from authoritative finding Validated: dates + dated MEMORY entries (NOT reconstructed from loose memory): 06-26 (ingestion-trace verification + stream() integrity doctrine + F-039 + F-038 evidence repair), 06-25 (F-037 CRT-only spine + F-038 rr=gaussian + mt5 v0.4.0), 06-24 (F-036 zone top_k inert), 06-19 (F-035 FX generalization + MT5 fetch layer), 06-18 (F-032/F-033/F-034 panel+carry nulls + perp fetch + flow-context M1 + E-001), 06-17 (F-030 regime-LEVEL + F-031 E-001E governance catch), 06-16 (Framework Registry), 06-15 (F-029 lookahead-benign + Config-First doctrine frozen), 06-14 (F-028 PNF-v1 REJECTED/FROZEN), 06-13 (F-027 HTF Program 3A — was missing). Added Eras item 5 (research-falsification sweep + config-first + integrity hardening, 06-06→06-26). VALIDATION: all new data rows well-formed 4-col; no dedicated timeline test exists (docs-only truth-maintenance per §6.2 rule 6).
Belief Update / ROI / Goal:
  Goal: keep the chronological index (timeline.md) synchronized so cold-start sessions can place every finding in time (§6.2 rule 6).
  Belief: the timeline had drifted 13 days behind the findings ledger (F-027…F-039 absent); it is now current through 2026-06-26.
  Knowledge ROI: medium — no new conclusions, but restores the "what changed when" traversal path the knowledge-map depends on; prevents future sessions re-deriving the 06-14…06-26 arc from scattered findings.
  Action: timeline current; the earlier deviation (skipped lone F-039 row to avoid a misleading gap) is now fully resolved by backfilling the whole window.
Open Questions: none — the 06-20…06-23 gap is real (no recorded session work those days), not missing rows.
Next Step: none; timeline backfill complete.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Verified the user's five-era worldview-evolution "time replay" + persisted it (with an Era-6 forward-look) into PROJECT_HISTORY.md; recorded an E-001 self-correction.
Decision/Output: Appended two append-only sections to docs/PROJECT_HISTORY.md (which previously stopped at 2026-06-10): "## Worldview Evolution — Five Eras (Apr–Jun 2026)" (belief→built→reality→worldview-shift per era, the F-019…F-039 falsification arc, cited by F-id only — no path:line) + "## The Next Question — Era 6 (open hypotheses, not promises)" (entry-info bottleneck F-025, execution-model constraint F-030, intelligence-not-scarce F-001, reopen-only-via-new-ontology; candidate untested threads = event-driven/transition ontology, execution-microstructure, perp OI Program 7, metals calendar, the F-038 rr_fusion defect; honest null = instruments may be efficient at M15 → the compounding asset is the falsification machine itself). Added 2 TOC entries. VERIFICATION baked into the narrative: F-002 stated as F-002→F-021→F-017 (selection collapsed to session, session non-improvable OOS), F-021 exact reject counts (ZONE 0/110, SCORE 2/110 noise p=0.327, SESSION 110/112≡ALL). GATES: test_doc_citations + test_topic_docs + test_current_findings = 13 passed; grep guard confirms F-id/doc-name citations only (no path:line). Authority caveat (§6.5) explicit: Era 6 = research-framed open questions, no promised edge.
Belief Update / ROI / Goal:
  Goal: preserve the project's *intellectual* evolution (the why behind the findings) so cold-start sessions inherit the worldview, not just the conclusions (§6.1 zero intelligence loss).
  Belief: the repository's deepest change is its QUESTION — from "build a trading system" to "build a machine that discovers whether its own beliefs are false." The five-era arc is accurate to the ledger.
  Knowledge ROI: high — captures meaning (the belief→falsification trajectory), which §6.1 says is the non-recoverable asset; future sessions can place any finding in its worldview context.
  Action: narrative persisted; Era 6 left as falsifiable open questions, not a roadmap.
E-001 SELF-CORRECTION (mandatory phrase): "Caught me overclaiming; I owe you a correction." I flagged the user's "F-021 Score: 2 rejects" as drifted; verification (current-findings.md:305) shows SCORE = 2/110 rejects EXACTLY — the user was precise, I was wrong. The persisted narrative carries the correct figure; the false flag lived only in chat and is retracted here (no recorded artifact carried it).
Open Questions: none for this task; Era-6 threads are pre-registration candidates, not commitments.
Next Step: task complete; PROJECT_HISTORY.md now current through 2026-06-26.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: v0.5.0 "Commission Semantics Verified" COMPLETE — IC Markets Raw, Case C, zero kernel change; project AT REST
Decision/Output: Executed the Level-3 commission sprint end-to-end on IC Markets Raw (Raw Trading Ltd/ICMarketsSC-Demo/52935582, hedging mm2). Gate 0 PASS (commission_charged=True, Σ −0.23 across 5 deals). Form = Case C (commission split entry+exit, FOLDED onto volume>0 trade deals; NOT a separate Case-B zero-volume deal). Kernel net_pnl=Σ(profit+swap+commission) summed it correctly: episodes net_pnl −0.08/−0.15 = the per-deal commissions; verify PASS (net_pnl_diff ≈2.8e-17, volume_diff 0.0, manifests OK), 2 pos→2 eps. 74 green. ZERO kernel change. Committed 59773f7 (MIGRATIONS + BROKER_VALIDATION) + memory. Plan files (working + tracked docs/implementation_plan) marked COMPLETE/AT-REST with earned scope + three reopen conditions.
Belief Update / ROI / Goal: Goal: close the strongest remaining broker-semantics assumption (commission==deal.commission) with real evidence. Belief: CONFIRMED on the first commission-charging AND first non-MetaQuotes broker — the kernel handles commission (Case C, split entry+exit) with zero change; reconstruction now validated across TWO broker families {MetaQuotes hedging/netting/INOUT/folded-swap + IC Markets commission}. Knowledge ROI: high — a major-yet-bounded result; the abstraction boundary held against a new broker family with no mutation. Earned scope is "validated across observed MT5 broker families and semantic forms", NOT "broker-independent". Action: kernel frozen; project AT REST.
Open Questions: Does any broker emit a SEPARATE zero-volume commission deal (Case B)? Exchange-margin mm1 semantics? Non-FX MT5 products?
Next Step: None operational. Reopen only on: (1) a Case-B separate-commission broker, (2) an mm1 exchange account, or (3) a real verify FAIL. Until then, preserve scope as earned.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: mt5_analytics v0.6.0 Post-Trade Intelligence Layer — first consumer of the truth engine (read model, info-not-authority)
Decision/Output: With broker validation external-evidence-gated, moved UP the stack. Built mt5_analytics/analytics/ (new pure read-model subpackage): insight_report.py composes src/analytics/metrics_oracle primitives (NO new metric math) into a frozen InsightReport — ExitEfficiency (capture/giveback), AdverseEfficiency, CostDrag (Σcommission/swap + gross-vs-net expectancy off PositionEpisode first-class fields, NO schema bump), RiskAdjusted (sharpe/recovery/DD/percentiles), AttributionReport (session×regime×duration, self-describing AttributionBucket with SufficiencyStatus Enum — below min_n=30 makes NO claim, expectancy=None), ConcentrationReport (top_n + Herfindahl effective_n=1/Σpᵢ²). Wired via ui/dashboard_data.insight_summary. 11 new tests, 85 total green. Real-data smoke (ecn/, n=2): correctly INSUFFICIENT, surfaced real cost-drag (commission −0.23 → net expectancy −0.115/trade). Committed 2ae1244 + MIGRATIONS note + memory. Multi-LLM review hardened the design pre-build (frozen typed contract over dict, Enum over strings, effective_n, self-describing buckets, structural read-model boundary).
Belief Update / ROI / Goal: Goal: convert validated execution truth into economically-meaningful post-trade intelligence (§6.1 — artifacts become value only as meaning). Belief: the truth engine now has its first real consumer; metrics_oracle already had every primitive (compose, don't reimplement); the sufficiency guard makes underpowered claims structurally unreadable (expectancy=None), preventing the #1 failure mode (humans trusting low-N stats). Knowledge ROI: high — crosses the boundary from "can we reconstruct reality?" to "what does it teach us?" while holding the information-not-authority line structurally. Action: layer is capability-complete; real insight awaits real trading volume (demo-N correctly reads INSUFFICIENT).
Open Questions: Will future ML/clustering need realized_r_net as a first-class feature (v0.7+ schema bump)? Optional Streamlit card not built.
Next Step: None required — v0.6.0 capability-complete. Optional: Streamlit card (aggressive INSUFFICIENT rendering); or pay down the due consolidation debt (session-log/findings/memory). Real insight accrues when real (non-demo) trade history exists.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: v0.6.0 marked SHIPPED/AT-REST; architectural transition recorded; bottleneck reframed to N (real volume)
Decision/Output: Recorded v0.6.0 (commit 2ae1244) as capability-complete/at-rest in both plan copies (working + tracked docs/implementation_plan). Captured the architectural transition (v0.1–v0.5 = truthfulness "was reality reconstructed correctly?"; v0.6 = economic meaning "what does reality imply?") and the demonstrating result (gross 0.0 + commission −0.23 → net expectancy −0.115/trade — a statement only the intelligence layer can make). Locked the read-model boundary as a PERMANENT structural invariant (MT5→truth→features→insight→HUMAN, never insight→decisions; no recommendation/optimization module). Listed the optional non-foundational v0.7 menu (Streamlit cards / JSON export / markdown report / realized_r_net feature only if ML needs it). Doc-hygiene commit only; no code.
Belief Update / ROI / Goal: Goal: keep the resting state honest and the doctrine enforced. Belief: the mt5_analytics subsystem is capability-complete end-to-end (truth engine + intelligence layer); the limiting reagent is now N (real, commission-bearing, non-demo trade volume), not software — the sufficiency gates correctly stay shut on demo-N. Knowledge ROI: low (hygiene/closeout), but the permanent read-model invariant is the highest-value durable guard against future doctrine drift. Action: stop building; accumulate real executed reality before the gates open.
Open Questions: When will real non-demo volume accrue to open the sufficiency gates? Will ML ever need realized_r_net?
Next Step: None software-side. User accumulates real commission-bearing trade volume; the intelligence layer then produces actual (SUFFICIENT) insight. Optional v0.7 items build only on concrete need.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Phase-C terminal framing recorded — project enters Reality Accumulation (data-gated, not software); final bookkeeping
Decision/Output: Captured the user's "software-complete ≠ project-complete" reframe as the terminal state in both plan copies: ~90-95% software / ~5-10% data maturity (Capability ≫ Data). Phase A (truth, v0.1-0.5) + Phase B (meaning, v0.6) DONE; Phase C (belief calibration — "do live outcomes match research?") NOT STARTED, data-gated. The real v0.7 is a campaign not a release: Reality Accumulation, target 50-100 commission-bearing qualified-strategy human-approved MT5 trades. F-010 BRIDGE made explicit (epistemically scoped): mt5_analytics can CLOSE F-010 (live PnL UNVERIFIED) but v0.6 supplies only the live-actual HALF — also needs spine-strategy-driven trades + an unbuilt, data-gated Reality-vs-Research Comparator. Recorded the explicit DO-NOT-BUILD list (comparator/ML/DuckDB/clustering/MonteCarlo/drift/realized_r_net — all require N), the Bookkeeping≠Progress≠Learning compounding hierarchy, and the three reopen conditions (data N≥30 / semantic Case-B|mm1 / verify FAIL). Accepted the correction that doc-hygiene is bookkeeping not progress; this is the FINAL closeout commit.
Belief Update / ROI / Goal: Goal: avoid mistaking software/doc completeness for project completeness. Belief: the mt5_analytics subsystem is capability-complete and AT REST; the scarce resource is now executed reality (N), and only reality-accumulation + research-vs-reality calibration compound strongly from here. The system needs evidence, not architecture. Knowledge ROI: high (strategic) — names the real bottleneck and the F-010 bridge that connects the sidecar to the main findings ledger, and pre-commits to NOT building speculative software. Action: STOP touching software; hold until a reopen condition fires.
Open Questions: Will qualified live performance converge to backtest expectations (F-010) once N≥30 exists? — the central empirical unknown, unanswerable without real trades.
Next Step: USER-side, not software: run the Phase C Reality Accumulation campaign (qualified-strategy commission-bearing trades). Re-entry trigger: point the pipeline (rebuild→verify→build_insight) at the live account; the sufficiency gates decide when a claim is earned. No further software/docs until a reopen condition.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: F-038 RR fusion Gaussian-duplicate defect — FIXED (rr_fusion disabled on active config, not retrained)
Decision/Output: Returned to spine research per user request, chose to fix F-038 (a confirmed defect, not a closed research question) over reopening edge-discovery Programs 1-2 (both KILLED/FROZEN) or chasing a new interpreter ontology. Two-agent exploration confirmed the mechanism: `engine_runner.py:698-709` legacy path starves rr_fusion's Mahalanobis model to 3/38 features (conf~1e-88); the shipped Fix A full-vector flag (default false) still only reaches conf~5e-5, five orders below the 0.3 bypass threshold — the underlying `models/rr_model.json` (trained 2026-05-24, BNB-specific) is OOD/mis-calibrated even fed correctly. Plan agent designed phased options; user (correctly) rejected my first ExitPlanMode attempt, demanding direct code re-verification against the current branch rather than trusting subagent summaries — re-read `rr_fusion.py` + `engine_runner.py:660-734` myself and confirmed the guard chain is structurally safe (disabling never reaches construction, base RREngine flows through unmutated, no metadata key, grep-confirmed no downstream consumer breaks). Shipped: flipped `engine_runner.rr_fusion.enabled` false in `configs/production/v2_multi_2026_04.json` (hash-neutral — confirmed `_compute_hash.py` only hashes the literal `params` dict, untouched); added `test_rr_fusion_disabled_is_base_rr_identity` (full-object identity, not just score) to `tests/test_engine_runner_rr_fusion.py`; fixed an unrelated pre-existing test-harness gap in the same file (`_build_runner` never set `_rr_fusion_full_vector`, masking `test_rr_fusion_applies_score_before_fusion`'s real assertion behind an AttributeError fallback). Updated F-038 in `docs/current-findings.md` (Confidence Likely→Certain, FIX SHIPPED note) + CLAUDE.md Repository Truths Index row + new Funding Ledger entry "RR Fusion Model Retrain/Recalibration — RESEARCH" (Option B explicitly deferred, gated on ΔG001 + promotion_manager). Verification: rr_fusion test file 6/6 green; determinism/parity suites (replay + bitnet) 6 passed/1 pre-existing xfail; full suite 772 passed before hitting one known pre-existing red (`test_agents_path_alignment`, documented in memory as already-red since 2026-06-11, unrelated AGENTS.md path-string drift) — re-run minus that deselected test in progress.
Belief Update / ROI / Goal: Goal: restore fusion integrity (3-4 genuinely independent signals) before any further spine work builds on a silently-doubled Gaussian. Belief: F-038 is now CLOSED at the production-behavior layer (Certain) — the duplicate is structurally impossible while disabled — while the model-quality question (Option B retrain) stays explicitly open as ungated future research, correctly separated per the §6.5 Authority Ladder (no model earns authority by existing/running). Knowledge ROI: high — fixes a real, previously undetected fusion-weighting bug with near-zero blast radius (one config flip, structurally provable safe) rather than spending a multi-day retrain on a model that's never demonstrated value. Process lesson reinforced: when a defect's root cause was established by subagent exploration, a human reviewer correctly demanded fresh first-party code verification before accepting the fix as ready — that bar (code evidence over architectural memory) should apply to all future fix-design plans, not just this one.
Open Questions: Will the full-suite re-run (minus the known pre-existing red) come back fully green? Should F-038's retrain track ever be funded, or does the disabled state stay permanent given RR never had demonstrated authority?
Next Step: Confirm the deselected full-suite run is green; report completion to user. No further code changes planned unless the re-run surfaces something new.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Execution Quality Observatory (exec_telemetry/, Phase C-op) shipped — execution-truth capture, OPERATIONAL-ONLY
Decision/Output: User chose Option 2 (automated demo execution = execution science, NOT edge/F-010 validation, since the spine is research-null F-019…F-039), scope-reduced to a telemetry extension + read-only report (NO router/copier/allocator). Built new top-level exec_telemetry/ (the SECOND truth: execution truth, sibling of mt5_analytics financial truth): M0 frozen ExecutionEvent schema + shared RETCODE_NAMES; M1 trade_generator._send captures slippage/latency/retcode per order behind --exec-log into runtime/exec_telemetry/<company>_<server>_mm<mm>/orders.jsonl (gitignored, margin-mode-keyed; fixed script-path import via sys.path repo-root insert); M2 report.py build_exec_report (frozen ExecReport per broker: fill-rate, retcode histogram, side-normalized adverse-slippage + latency distributions; reuse metrics_oracle median/percentile; OPERATIONAL-ONLY no profit/expectancy/R; v0.6 sufficiency gating). 8 new tests + 85 mt5_analytics green; generator without --exec-log byte-identical. Live IC Markets smoke captured real +15pt adverse buy slippage + multi-second fill latency (facts the deal-based truth engine cannot see); report correctly INSUFFICIENT at n=2. Committed 5272138. Incorporated all user tightenings: rename to Execution Quality Observatory, frozen ExecutionEvent schema, margin-mode-keyed path, telemetry→HUMAN-never-decisions invariant, no profit metrics.
Belief Update / ROI / Goal: Goal: characterize execution risk separately from strategy risk so a future qualified strategy plugs into hardened execution infra (only one new unknown, not two). Belief: execution-time facts (slippage/latency/retcode) are a distinct, permanently-lost-if-uncaptured truth that the deal engine structurally cannot provide — now captured + summarized, operational-only, sufficiency-gated. The +15pt slippage / multi-second latency on a demo are real operational findings (though n=2=INSUFFICIENT). Knowledge ROI: high — adds the SECOND truth axis (execution) and de-risks execution ahead of any strategy, while holding every doctrine boundary (no edge claim, no decision feedback, demo-gated). Action: capability shipped; real distributions await N≥30 per broker (same data-gate as everything else).
Open Questions: Do per-broker slippage/latency distributions differ materially once N≥30 (the demo Python-bridge latency was multi-second — real?)? Still no qualified strategy (F-019…F-039 wall).
Next Step: Accumulate per-broker exec telemetry (run --exec-log across demo brokers) until N≥30 unlocks the distributions; otherwise the system is again data-gated. No further software until a reopen condition.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: Program 4b/4c/4d candle-state transition frontier — F-040, Stage-1 PASS / Stage-2 FAIL, Program 4 CLOSED
Decision/Output: User proposed a "candle-state edge discovery engine" (state×MTF×RR×hold sweep hunting ≥70% WR). Recon (3 Explore agents) reframed it: the discipline already exists (HypothesisRunner/forward_walk/EdgeAggregator/7-gate QualificationGate); 7/10 hypotheses are already-falsified directional archaeology (F-019/020/021/025/027/035, F-023, F-026); Program 1 KILLED; no M5 on disk (resampler upward-only). User mandated NEW FRONTIER ONLY + M15-base + expectancy-first(WR reporting-only) + reuse-kernel, then added Programs 4c/4d, frozen Stage-1 thresholds, and a Program-4 closure rule. Built additive src/research/candle_state/ (encoder.py CandleStateEncoder, mtf_conjunction.py MultiTFConjunctionBuilder+key_series, transition_target.py, info_robustness.py, reporting.py) + src/research/hypotheses/compression_breakout.py (Stage-2 consumer, registered) + 3 drivers (transition_information/qualify_transitions/candle_state_report) + 28 tests (199 research-suite green, determinism×2). Pre-registered docs/research/preregistration-program-4bcd.md BEFORE running. RESULT: Stage-1 (M15∧H1∧H4 conjunction, 2000-perm) — 4b-vol/4b-range/4d-transition all p≈0.0005, UNIVERSAL (crypto+FX), persistent (half-life 8, 2024→2025 retention 0.87/0.99/0.67); 4c-persistence p≈0.97 REJECTED. Stage-2 (compression_breakout, intrabar_fixed+12bps) 0 PROMOTE, well-powered REJECT (crypto E −0.25…−0.60R WR≈0.33; FX cost-dominated PF 0.02–0.08). Filed F-040 (ECONOMIC/Likely) + Funding-Ledger "Program 4 — KILLED" + CLAUDE.md Repository Truths Index row + memory.
Belief Update / ROI / Goal: Goal: test the last reopen-legal candle frontier without archaeology. Belief: the volatility/range-EXPANSION + compression→expansion TRANSITION channel (F-030's untested Program-4b successor) IS informative/universal/persistent but is NOT economically consumable by a spot directional consumer — binding constraint = EXECUTION MODEL (can't express long-vol), not predictability; extends F-030. Knowledge ROI: high — the textbook Stage-1-PASS/Stage-2-FAIL ("information exists, market prices it efficiently, no tradeable directional edge") is a stronger conclusion than "no information," and crypto Stage-2 WR≈0.33 independently reproduces F-023. E-001 guards held: did NOT isolate MTF-conjunction's incremental info beyond single-TF vol → no MTF-novelty claim (consistent with vol memory H_atr=0.885); FAIL scoped to the directional construction, not "no instrument could monetize." Action: Program 4 CLOSED per pre-registered rule — no further candle-state work without new data / new domain / new (non-directional) execution architecture; kernels preserved as reusable assets.
Open Questions: Would a non-directional execution architecture (long-vol/straddle proxy) monetize the confirmed expansion forecast? (Separate program, needs a fresh thesis — NOT a 4e parameter pass.) Does the MTF conjunction add info beyond single-TF M15 vol (not isolated this pass)?
Next Step: Commit when the user asks (branch `patch`). No further software here — Program 4 is closed and the system is again at the F-019…F-040 directional-null wall.
---
📝 SESSION LOG ENTRY
Date: 2026-06-26
Topic: MT5 M5 fetch + STRICT zero-tolerance session-aware gap gate — built, tested, live-probe-validated
Decision/Output: User asked to fetch M5 via MT5 with a HARD-STOP gap gate ("can't train on inconsistent data"; OHLCV mandatory). Recon (2 Explore agents): the MT5 fetch CLI already supports M5 (scripts/data/fetch_candles_mt5.py via MT5CandleFetcher), and dataset_integrity.validate_dataset already RAISES + is session-aware (_analyze_gaps counts only TRADABLE missing; largest_gap_span = tradable_missing*bar_minutes, so FX weekends/rollover produce 0 tradable-missing). Only gaps vs the requirement: lenient tolerance (2%/100) + no holiday awareness. User decisions: FX+metals only (EURUSD/GBPUSD/AUDUSD/USDJPY/EURCAD/XAUUSD), no crypto, native M5+M15+H1+H4. Built (all parity-preserving): (1) validate_dataset gains cfg_override (None=byte-identical); (2) _is_tradable consults session_calendar.holidays (empty default=parity, universal non-tradable dates); (3) config dataset_integrity.strict_fetch {max_missing_pct:0,max_single_gap_candles:0,max_gap_span_minutes:0} + session_calendar.holidays:[] (hash-neutral, not params); (4) scripts/data/fetch_and_verify_mt5.py — fetch each symbol×TF, strict-verify with the override, print exact missing TRADABLE timestamps, QUARANTINE failures to data/_rejected/, nonzero exit. 7 new tests (crypto hole→raise, FX weekday hole→reject, FX weekend complete→approve, holiday-listed→approve, OHLCV NaN→raise, default-lenient-vs-strict divergence, cfg_override=None parity); 69 data_ingestion/integrity/resample tests + 66 config tests green (parity confirmed). LIVE PROBE: terminal up, fetched EURUSD M5 1440 bars; clean week → strict APPROVE, exit 0; scratch-dir path correctly REJECTs on enforce_path_consistency. Full 6×4 2yr fetch launched (background) — expected to surface XAUUSD/FX holiday gaps for reviewed holiday-list curation (per F-035 XAUUSD 26h holiday gap).
Belief Update / ROI / Goal: Goal: enable consistent M5 (+native ladder) data with an integrity guarantee strong enough to train on. Belief: the strict gate = the EXISTING session-aware gate with zero-tolerance thresholds + an explicit reviewed holiday list; this hard-stops genuine mid-session holes (crypto: any; FX: weekday holes) while NOT false-stopping legitimate weekend/holiday closures. Confirmed live: clean FX M5 week passes, the mechanism is correct. Knowledge ROI: high-leverage reuse — ~zero new gap logic, just a config profile + a holiday hook + an orchestrator; the "any missing timestamp → stop" rule is correctly interpreted as "any timestamp missing where the market was OPEN." Action: run full fetch; bring surfaced holiday dates to the user for the reviewed holiday-list decision (do NOT auto-raise tolerance — that re-admits corruption); note directional candle frontier stays CLOSED (F-040) so new M5 use needs a new ontology.
Open Questions: Which surfaced gaps are genuine holidays (→ add to session_calendar.holidays) vs broker corruption (→ data unusable)? Does the broker emit hour-21 rollover M5 bars (extra, harmless) or omit them (could matter for FX)? Re-fetched FX M15 overwrites the F-035/F-040 corpus (git-recoverable).
Next Step: Review full-fetch output; curate holidays with the user; re-run until ALL CLEAN; only then is the M5 ladder trainable. Commit when the user asks.
---

---
📝 SESSION LOG ENTRY
Date: 2026-07-01
Topic: F-038 deploy committed (670a860) — wrap-up
Decision/Output: Commit 670a860 landed on `patch` (5 files: ACTIVE_VERSION flip, finding_dependency_audit.md, current-findings.md, rotated assistant_project.md + new session-log archive). Pre-commit hook green (71 passed/1 skipped). Fixed a stray-`@` commit subject (PowerShell here-string `@'...'@` is invalid in the Bash tool — used `git commit --amend -F msgfile`). Not pushed. Advisory consolidation signals: findings 39/30 DUE, memory 55/45 DUE (non-blocking).
Belief Update / ROI / Goal: none (mechanics — deploy + commit hygiene).
Open Questions: ~80 unrelated working-tree files + .env still uncommitted (owner's scope). Push not requested.
Next Step: optional — push patch; or run consolidate-memory / retire settled findings per advisory signals.
---

---
📝 SESSION LOG ENTRY
Date: 2026-07-02
Topic: Ordered ops 1-3 — secret-scrub + push, track fetch CLI, known_gaps accept-list
Decision/Output: Executed the approved ordered plan. STEP 1 (push): `git push origin patch` was REJECTED by GitHub push-protection — a live Groq API key is in `.env` at commits 278d993/eb64269 (PRE-EXISTING, from prior sessions; both unpushed). Halted (did NOT bypass — publishing a live secret is an irreversible leak). User chose "scrub + push". Correction: I'd earlier called .env "git-tracked, recoverable" — it IS tracked but GITIGNORED (line 9); tracked before the ignore rule. Scrubbed SAFELY without touching the 332 uncommitted working files: backup branch (backup/pre-env-scrub-20260702=670a860) → isolated `git clone` of committed history → `git filter-branch --index-filter "git rm --cached --ignore-unmatch .env" --prune-empty` (60→59 commits, .env fully removed, verified) → verified FF (GitHub tip is ancestor) → `git push gh patch` SUCCEEDED (5897209..05d4ad0; secret gone, only LFS size warnings) → realigned main working-tree-safe `git reset --mixed origin/patch` (331 files preserved, .env now untracked+on-disk) → deleted the clone (held secret). My F-040 work preserved as f0ce74b. STEP 2: `git add -f scripts/data/fetch_and_verify_mt5.py` (over the collateral bare-`data` ignore), commit 8bff9e3, pushed. STEP 3: known_gaps accept-list — session_calendar.known_gaps [{symbol?,from,to,reason}] timestamp-RANGE allowlist so a reviewed few-bar broker outage is accepted WITHOUT whole-day holiday marking; _is_tradable checks it first (all modes), validate_dataset parses per-symbol via new _parse_known_gaps, CLI diagnostic honors it; empty=parity. +2 tests; 71 data_ingestion+integrity green. USER MUST ROTATE the Groq key (compromised — in local history + GitHub-scanned; out-of-band, only they can).
Belief Update / ROI / Goal: Goal: publish the work + a clean, trainable corpus without leaking secrets or losing uncommitted work. Belief: the repo carried a live secret in unpushed history (prior sessions) — GitHub push-protection caught it; scrubbing via an isolated clone kept the 332-file working tree untouched (reset --mixed is the safe realign). Knowledge ROI: high — turned a blocked push into a clean history + a reusable precise accept-list, with zero work lost. Action: STEP 4 next (full bulk fetch + review loop); user rotates the Groq key; optionally delete backup branch (holds the old secret locally) once satisfied.
Open Questions: Which residual micro-gaps after re-fetch are genuine broker outages (→ known_gaps) vs corruption (→ stay blocked)? Delete backup/pre-env-scrub-20260702 (contains the old secret in history)?
Next Step: Run STEP 4: `fetch_and_verify_mt5.py --discover-crypto`; review quarantine; propose known_gaps entries for approval; re-verify.

---
📝 SESSION LOG ENTRY
Date: 2026-07-02
Topic: STEP 4 bulk fetch + review — FX/metals ALL-CLEAN via known_gaps; crypto CFDs rejected → Binance next
Decision/Output: Ran `fetch_and_verify_mt5.py --discover-crypto` (92 fetches: 6 FX/metals + 17 crypto CFDs × M5/M15/H1/H4 → data/mt5/). Result: 84/92 REJECT (only 8 clean = the H4/H1 coarse TFs). ROOT CAUSE separated: (a) FX/metals M5/M15 have a BOUNDED set of genuine broker feed outages — 1-3 windows/M15-file, 4-12/M5-file, all brief (1-7 bars) on normal trading days, clustering broker-WIDE on specific dates (03-25/04-01/04-22/05-01/05-26/06-15/06-29/07-01 hit 4-6 symbols simultaneously) = real outages, not corruption; (b) crypto CFDs have STRUCTURAL gaps up to 551 bars (late-listed altcoins, halts) = feed genuinely incomplete over 2yr. USER decisions: accept-list FX/metals (generate from detected windows) + get crypto M5 from BINANCE instead of gappy CFDs. Generated 17 known_gaps entries (broker-wide=global, single-symbol=scoped; XAUUSD 2026-05-25/06-19 flagged as gold US-holiday early-closes) into config. RESULT: all 24 FX/metals files (6×M5/M15/H1/H4) now APPROVE; moved into data/mt5/. 68 crypto CFD files remain quarantined (abandoned for Binance). 71 data_ingestion+integrity tests green (parity). NOTE: 2026 US holidays after April (Memorial/Juneteenth/July-4) not in holidays list (old data ended May) — currently absorbed via known_gaps for XAUUSD; could migrate to holidays later.
Belief Update / ROI / Goal: Goal: a strict, trainable multi-TF FX/metals corpus + a clean crypto source. Belief: zero-tolerance is the RIGHT gate but exposes that (i) FX/metals feeds have a small, reviewable set of real broker outages (now accept-listed → clean), and (ii) broker crypto CFDs are too structurally gappy for research → Binance (24/7 spot) is the correct crypto source. Knowledge ROI: high — the gate converted "is this data usable?" into a precise, auditable answer per instrument; FX/metals corpus is now COMPLETE and clean, crypto redirected to a sound source. Action: commit the accept-list; build Binance crypto M5 (extend fetch_crypto_ccxt) as the crypto provider.
Open Questions: Migrate the 2026 US-holiday early-closes from known_gaps to a proper 2026 holidays list? Delete abandoned crypto CFD files in data/mt5/_rejected? Delete backup/pre-env-scrub branch (holds old secret)?
Next Step: Build + run Binance crypto M5 (24/7) through the strict gate as the crypto corpus; then the multi-TF FX/metals + crypto corpus is complete.
