# Pre-Registration — Program 4b / 4c / 4d (Candle-State Transition Frontier)

> **Status:** PRE-REGISTERED (written before any run). Authority: research/docs only (§6.5
> Authority Ladder). This document FREEZES the hypotheses, the targets, the thresholds, and the
> closure rule **before** results exist, so a null cannot be quietly re-spun into a finding and a
> hit cannot be threshold-shopped. Enforced socially by the Epistemic Integrity ritual (Program
> E-001, [`docs/governance/EPISTEMIC_INTEGRITY.md`](../governance/EPISTEMIC_INTEGRITY.md)).

## Context & scope

Program 1 (next-bar **directional** ontology) is KILLED across four falsifications (F-019/020/021/
025) and generalized to HTF (F-027) and FX (F-035); morphology clustering (F-023) and
sweep-continuation (F-026) are also dead. Re-running those is forbidden archaeology. The only
reopen-legal frontier inside the candle-state proposal is **non-directional** and was named but
never built: the F-030 **TRANSITION** successor. This program tests it under the fixed standard
(M15 base → H1/H4 derived, intrabar_fixed exit, 12bps, the existing M4 QualificationGate).

**Two-stage gate.** Stage 1 = information (non-directional, Authority Level 1). Stage 2 = economic
(`compression_breakout` through the unchanged QualificationGate, Authority Level 2). A conjunction
reaches Stage 2 ONLY after clearing all Stage-1 thresholds below.

## Hypotheses (all NON-DIRECTIONAL; conditioning cell = the M15∧H1∧H4 conjunction key)

| ID | Conditioning (cell) | Forward target | H0 (null) |
|---|---|---|---|
| **4b** | conjunction key at t | forward **vol/range expansion**: mean(ATR/range over t+1..t+k)/value_t > θ | the conjunction carries no information about forward expansion (IG ≤ permutation null) |
| **4c** | conjunction key at t | **state persistence**: key unchanged across t+1..t+k | conjunction is not differentially sticky beyond base rate |
| **4d** | conjunction key at t (compression bars) | **regime transition**: COMPRESSION→EXPANSION within k | conjunction carries no compression→expansion timing info |

Horizons swept: **k ∈ {1, 2, 4, 8}** bars (also the half-life curve). θ pre-set: vol/range
expansion θ = 1.5 (expansion = forward typical > 1.5× current). Permutations: 2000, seeded.

## FROZEN Stage-1 promotion thresholds (no post-hoc changes)

A conjunction advances to Stage 2 only if it clears **all four**:

| Gate | Threshold |
|---|---|
| Permutation significance | p ≤ **0.05** |
| MI/IG retention (train 2024 → test 2025) | ≥ **0.50** |
| Information half-life | IG(h) ≥ 0.5·IG(h₀) at **h ≥ 4** bars |
| Cross-market | crypto PASS + FX PASS ⇒ **UNIVERSAL** · crypto PASS + FX FAIL ⇒ **DOMAIN_SPECIFIC** · else ⇒ **REJECTED** |

Changing any threshold requires a NEW pre-registration, not an edit to this file.

## Program-4 closure rule

Program 4 is declared **CLOSED** once 4b **and** 4c **and** 4d each resolve to either
(Stage-1 PASS + Stage-2 FAIL) **or** (Stage-1 FAIL), under the fixed standard. After closure, no
further candle-state work may be promoted without **new data**, a **new market domain**, or a
**new ontology** — 4e/4f/4g parameter variants are archaeology (mirrors the Program-1 closure).

## Epistemic Integrity 6-question pre-check (per E-001)

1. **What artifact supports a future claim?** The deterministic Stage-1 JSON
   (`results/research/candle_state/transition_information.json`) + Stage-2
   `qualify_transitions.json`, both byte-reproducible (file:line in the report). No claim without it.
2. **Could INSUFFICIENT explain it?** Yes — compression bars are sparse; per-conjunction cells may
   be underpowered. Underpowered cells make NO claim (gate INSUFFICIENT, n<30), never REGIME_*.
3. **Am I upgrading sign noise into meaning?** Guarded: IG significance at large N is necessary,
   not sufficient (F-020 lesson). Stage-2 economic gate, not Stage-1 IG, is the promote authority.
4. **Statistical or economic?** Explicitly separated: Stage 1 = statistical/information (Level 1);
   Stage 2 = economic (Level 2). A Stage-1 PASS earns docs/shadow only — never sizing/fusion.
5. **Parent stronger than children?** The rollup will assert E-001E: a "transition informative"
   parent requires its powered child cells to support it; all-INSUFFICIENT ⇒ INSUFFICIENT, not a
   positive verdict.
6. **Would I phrase it differently after raw counts?** The report prints n per conjunction cell and
   per stage; verdicts are written only after inspecting those counts.

## Expected outcome (prior, not a result)

Stage-1 PASS / Stage-2 FAIL ≈ 55% · Stage-1 FAIL ≈ 35% · Stage-1 PASS / Stage-2 PASS ≈ 10%
(F-030: vol predictable but not consumable; F-025: bottleneck is entry information). Either way,
resolving 4b+4c+4d triggers the closure rule. The deliverable is a definitive closure, not a
strategy.
