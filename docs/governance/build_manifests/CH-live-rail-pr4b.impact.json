{
  "change_id": "CH-live-rail-pr4b",
  "objective": "PR-4b of the authorized live-rail repair path: LiveRailFeeder fills REQUIRED_LIVE_TRADE_DATA_KEYS from FeaturePipeline (last row) after required_warmup_rows(); FeatureStore remains the live validator. No orchestrator, no hook.process caller, no orders, no ACTIVE_VERSION edit. User authorized 2026-08-19.",
  "change_classes": [
    "RUNTIME_DECISION_PATH_CHANGE"
  ],
  "production_behavior_changed": false,
  "affected_files": [
    "src/runtime/live_rail_feeder.py",
    "tests/test_live_rail_feeder.py",
    "docs/governance/build_manifests/CH-live-rail-pr4b.impact.json",
    "assistant_project.md"
  ],
  "affected_feature_ids": [],
  "affected_models": "none — feeder reads FeaturePipeline; does not change CANONICAL_FEATURES",
  "affected_production_config": "none",
  "required_checks_ack": [
    "tests/test_feature_math_lint.py",
    "tests/test_geometry_census.py",
    "tests/test_current_findings.py",
    "tests/test_live_rail_feeder.py"
  ],
  "unknowns": [
    {
      "question": "Is ready() at window==78 or window>=79?",
      "blocking": false,
      "resolution": "Pipeline finalize() DROPS the first required_warmup_rows() (78) bars. First valid last-row is bar index 78 (79 bars). ready() requires a valid last pipeline row, not merely len>=78. Tests pin this T-16 identity.",
      "followup": "PR-4c must not call hook.process until feeder.ready()."
    }
  ],
  "rollback_boundary": "Delete src/runtime/live_rail_feeder.py and tests/test_live_rail_feeder.py. No params hash, no ACTIVE_VERSION, no hook revert.",
  "authority_granted": "User authorized PR-4b 2026-08-19. Does not authorize PR-4c caller, dry_run=false, live money, or promotion."
}
