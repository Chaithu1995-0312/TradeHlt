# Four tracks the user asked for (2026-08-15)

Do not collapse these into one “make everything 100%” switch.

| Track | Ask | What “done” is | Authority | Status this turn |
|---|---|---|---|---|
| T1 | Use `mt5_analytics` / `oss_lab` / `tools` | GCMC **v2** = 100% file-map of those trees + each tree has a named job on T3/T4 | User authorized inventory + use as lab | **STARTED** |
| T2 | Control production | A config reaches production only via `ValidationReport.APPROVE` + `PromotionManager` + hash + `promotion_log.jsonl` | **I do not seize `ACTIVE_VERSION`** | **REFUSED as a grab.** Path mapped. |
| T3 | Make CRT CLOSED | Phase 1–8 re-cert (or equivalent) after F-074. `tests/test_crt_closure_report.py` currently **requires** `REOPENED`. | Construction protocol. No doc-flip. | **STARTED** (floors only) |
| T4 | Find an edge | Sense B: honest ΔG001 under a measurement contract | `P-GOAL-04` now authorized as **measure**, not invent | V1 n=3 · SOFF n=12 · **NS n=16**. All INSUFFICIENT. No edge. |

`oss_lab` is **RESEARCH_LAB_ONLY** (`oss_lab/governance/ARCHITECTURE_APPROVAL.md`). It does not control production.

`mt5_analytics` reconstructs broker deals. That is an honesty surface for T4, not a live order button.

`tools/` (TV forensic + BTC replay) checks whether CRT **means** the market picture. That feeds T3, not T2.
