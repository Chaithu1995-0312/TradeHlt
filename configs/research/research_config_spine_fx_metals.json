{
  "_doc": "Edge Discovery — SPINE x FX-MAJORS variant. Measures the full production decision spine (CRT/Gaussian/ZoneGate/RR -> Fusion -> RegimeGovernor -> Decision -> ExecutionPlanner -> UltronRiskGate) under the research truth standard, as the 'spine' hypothesis, across five FX majors. apply_signal_defaults MUST be false so the runner keeps the spine's own SL/TP geometry. Qualification knobs identical to research_config_fx_metals.json so spine verdicts are comparable to the toy family. The `spine` block is read by ProductionSpineSource (not by ResearchConfig). Expectation: the spine likely produces few/0 trades on FX (F-029) -> INSUFFICIENT; the toy family is the informative arm.",
  "harness": {
    "warmup": 30,
    "window_size": 64,
    "min_samples": 30
  },
  "forward_walk": {
    "max_forward": 40,
    "trail_mult": 0.5,
    "exit_model": "intrabar_fixed"
  },
  "signal": {
    "apply_signal_defaults": false,
    "sl_atr_mult": 1.0,
    "tp_atr_mult": 2.0
  },
  "costs": {
    "round_trip_bps": 12.0
  },
  "qualification": {
    "min_samples": 30,
    "expectancy_min": 0.0,
    "pf_min": 1.0,
    "oos_split": 0.3,
    "oos_retention_min": 0.5,
    "n_permutations": 2000,
    "significance_alpha": 0.05
  },
  "universe": {
    "data_dir": "data",
    "pattern": "*_M15.csv",
    "instruments": ["EURUSD", "AUDUSD", "EURCAD", "GBPUSD", "USDJPY"]
  },
  "spine": {
    "prod_version": "v2_multi_2026_04",
    "_prod_version_note": "Canonical, R1/R2-governed v2 (matches configs/production/ACTIVE_VERSION). v4_multi_2026_06 is NOT loadable on the `patch` branch.",
    "scorer_mode": "calibrated",
    "tp_target": "tp2"
  }
}
