{
  "_doc": "Edge Discovery -- XAUUSD, single month (2026-04), gated corpus_store flow validation. Seeded from research_config_shape_xauusd.json. Purpose is plumbing validation of the gated window+component-cost path end to end, NOT an economic claim: n on one instrument for one month is far below any threshold this repo treats as evidence (see docs/current-findings.md F-019...F-042). No finding is registered off this config's output. `costs.round_trip_bps` is declared but UNUSED when cost_model='component_measured' -- ResearchConfig.from_dict requires the key structurally regardless of which cost_model is active; the real cost basis for this run is costs.cost_model_manifest_path (SEM-015 measured broker calibration), not this number.",
  "harness": {
    "warmup": 30,
    "window_size": 64,
    "min_samples": 30
  },
  "forward_walk": {
    "max_forward": 40,
    "trail_mult": 0.5,
    "exit_model": "intrabar_fixed"
  },
  "signal": {
    "apply_signal_defaults": true,
    "sl_atr_mult": 1.0,
    "tp_atr_mult": 2.0
  },
  "costs": {
    "round_trip_bps": 12.0,
    "cost_model": "component_measured",
    "cost_model_manifest_path": "results/research/xauusd_mt5_cost_calibration/xauusd_mt5_cost_calibration_manifest_LATEST.json"
  },
  "qualification": {
    "min_samples": 30,
    "expectancy_min": 0.0,
    "pf_min": 1.0,
    "oos_split": 0.3,
    "oos_retention_min": 0.5,
    "n_permutations": 2000,
    "significance_alpha": 0.05
  },
  "universe": {
    "data_dir": "data",
    "pattern": "*_M15.csv",
    "instruments": ["XAUUSD"]
  },
  "window": {
    "start": "2026-04-01",
    "end": "2026-04-30T23:45:00",
    "lead_in_bars": 120,
    "tail_bars": 60
  },
  "job_kind": "unspecified"
}
