{
  "flow": "training",
  "title": "Training / Model Pipeline",
  "doc": "docs/architecture/service-boundary-map.md",
  "service_ids": ["S5", "S6"],
  "entrypoint": "training.train_pipeline",
  "modules": [
    "features.dataset_builder",
    "features.dataset_validator",
    "config_layer.rr.rr_dataset_builder",
    "training.trainer",
    "training.train_pipeline",
    "training.phase5_calibration",
    "training.evaluator",
    "core.model_registry"
  ],
  "keywords": ["dataset", "trainer", "calibration", "registry", "training"],
  "command_ids": ["training.auto_train", "training.discover_zones", "training.build_rr_dataset",
                  "training.train_rr_model", "training.opportunity_scanner", "training.phase5_calibration",
                  "training.train_pipeline", "tuning.auto_tuner_multi", "tuning.auto_tuner"],
  "inputs": ["opportunities.jsonl (unbiased scans) / RR dataset / M15 CSV"],
  "outputs": ["calibrated Gaussian + Zone + RR models",
              "versioned model files + registry entries (gaussian/zone/rr_registry.json)"]
}
