# data/ — LOCAL_BLOB (INV-050)

OHLCV and other corpus bytes. **Not in git.** See
[`docs/governance/GITIGNORE_SCHEMA.md`](../docs/governance/GITIGNORE_SCHEMA.md).

A clone does not get these files. Identity of the XAUUSD M15 frozen candidate
is the Dataset Identity record + `content_hash_sha256`, not this folder.

Re-running HEAD on a CSV found here is **RECOMPUTE**, not **RECOVER**.
